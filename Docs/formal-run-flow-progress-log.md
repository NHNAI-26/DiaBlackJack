# 정식 런 진행 기록

> 프로젝트: DiaBlackJack  
> 기획·통합 책임자: 이천서  
> 구현 예정 담당자: HONG  
> 작업 식별자: RF-00~RF-05  
> 버전: v1.1
> 상태: RF-01~RF-05 자동 검증 완료 · Unity 실화면 검증 대기
> 최종 갱신: 2026-07-30

> **현행 기획 이관 상태 (2026-07-30)**
> 일반전 뒤 카드 보상 화면을 제거하고 골드 정산 뒤 상점으로 바로 이동하며, 보스는 골드 결과 기록 뒤 즉시 승리한다. RF-01~RF-03 도메인과 RF-04 Runtime·진행 화면·기존 두 씬의 코드 연결이 구현됐다. Unity 실화면·씬 왕복 검증은 MCP 연결 복구 뒤 남아 있다.

> **현행 상점 규칙 변경 안내 (2026-07-29)**
> 카드 슬롯은 각각 재고 1개지만 방문 전체 구매 횟수에는 상한이 없다. 라이터와 위스키는 방문당 각각 1회 이용할 수 있고 같은 방문에서 둘 다 가능하다. 둘 중 하나 이상 이용한 방문은 다음 상점의 두 서비스 가격을 함께 한 단계만 올린다. GameScene MVP의 기본 2골드·상승 +1은 임시값이다.

> **Notion v0.7 골드 영향 (2026-07-29)**
> 적이 6종으로 재구성되어 RF-00의 3·3·4·6·10은 과거 임시값이 되었다. 현행 지급량 3·4·6·7·9·15는 RF-01B 카탈로그와 승리 정산에 반영됐다.

## 1. 기록 원칙

- 계획과 실제 구현을 분리한다.
- 실제 작업자가 구현·검증하기 전에는 계획 역할을 완료 기여로 기록하지 않는다.
- 각 단계마다 작업자, 변경 파일, 직접 결정한 문제, 대상·전체 테스트와 실제 화면 결과를 남긴다.
- AI 초안과 사람의 검토·결정을 구분한다.
- 전투·보상·상대 선택 내부 변경은 이유와 이천서 검토 여부를 기록한다.

## 2. 현재 기준선

| 항목 | 현재 상태 |
| --- | --- |
| 전투 스테이지 | 일반 2회와 마지막 보스 구현 완료 |
| 일반 전투 진입 | 상대 후보 2명 선택 구현 완료 |
| 기존 카드 보상 | RW 선택·건너뛰기 회귀 보존, 정식 Runtime 세션에서는 비활성 |
| 골드 상태·지급 | 보유·지급·소비·재시작·저장 왕복과 프로필별 승리 전투 1회 정산 구현 |
| 전투 후 이벤트 | 정식 런의 상점 2회 고정 순서 구현 |
| 상점 | RF-02 도메인과 RF-04 StageTest IMGUI 구매·제거·회복·나가기 연결 구현 |
| 최근 자동 기준선 | 전체 EditMode 626/626 통과 |
| RF-00 검증 | 문서 전용, Unity 재실행 안 함 |

현재 정식 Runtime은 `일반전 승리 → 골드 정산 → 상점 → 다음 상대 선택/보스`, `보스 승리 → 골드 결과 기록 → RunVictory`로 동작한다. 기본 `usesBattleRewards: true` 세션만 기존 `RewardSelection` 회귀를 유지한다.

## 3. 단계 현황

| 단계 | 담당 | 상태 | 완료 증거 |
| --- | --- | --- | --- |
| RF-00 | 이천서(AI 문서 보조) | 완료 | 문서 4종과 공통 기록 |
| RFM01 | 이천서(AI 구현·검증·기록 보조) | 완료 | 전용 3/3·전체 626/626·GameScene 화면 |
| RF-01A | 이천서(AI 구현·검증·기록 보조) | 완료 | 대상 7/7·Border/StageProgression 테스트 어셈블리 컴파일 |
| RF-01B | 이천서(AI 구현·검증·기록 보조) | 완료 | 적 6종 3·4·6·7·9·15·승패·중복·호환 대상 7/7, RF-01 전체 14/14 |
| RF-02 | 이천서(AI 구현·검증·기록 보조) | 완료 | 상점 제안·거래·가격·자동 카드·재시작 대상 11/11, 두 어셈블리 컴파일 |
| RF-03 | 이천서(AI 구현·검증·기록 보조) | 완료 | RF03 12건·RF-01~03 누적 37/37·두 어셈블리 컴파일 |
| RF-04 | 이천서(AI 구현·검증·기록 보조) | 코드 완료·Unity 검증 대기 | RF04 6건 컴파일·비Unity 5건 실행·누적 42/42·씬 직렬화 확인 |
| RF-05 | 이천서(AI 구현·검증·기록 보조) | 자동 검증 완료·실화면 대기 | 신규 8/8·RF 대상 49/49·전체 744/744·두 씬 정적 검증 |

## 4. RF-00 수행 기록

### 4.1 수행 내용

- 최신 `rule.md`, 전체 기획, SP·RW·EUI 문서와 현재 `PlayerRunState`, `StageProgressionSession`, Runtime·Controller를 대조했다.
- 작업 중 기준 문서가 갱신된 것을 확인하고 이전 휴식·상점·사건 3종 랜덤 초안을 폐기했다.
- 최신 기준인 전투 승리 골드, 상점 내 유료 휴식, 첫 개발의 두 상점 고정을 반영했다.
- 적별 골드 3·3·4·6·10과 상점 구매 4, 제거 3, 휴식 3골드·영혼 3을 프로토타입 임시값으로 정했다.
- 기존 전투 세션을 감싸는 정식 런 세션과 새 상점 도메인의 책임을 분리했다.
- 이천서를 기획·통합 책임자, HONG을 구현 예정 담당자로 기록했다.

### 4.2 산출물

- `Docs/formal-run-flow-design.md`
- `Docs/formal-run-flow-development-spec.md`
- `Docs/formal-run-flow-implementation-plan.md`
- `Docs/formal-run-flow-progress-log.md`
- `Docs/README.md`, AI 활용·팀 역할 기록

### 4.3 검증

- 문서 링크·작업 ID·책임자·정식 순서·골드·상점 용어 검색
- 최신 규칙과 현재 공개 API 대조
- 코드·테스트·씬·프리팹·패키지·외부 에셋 변경 없음
- 문서 전용 단계이므로 Unity 테스트와 Play Mode는 재실행하지 않음

### 4.4 AI 활용과 사람 결정

AI는 저장소 검색, 규칙 변경 감지, 현재 구조 대조와 문서 초안을 보조했다. 인수인계 범위, 임시 경제 수치, 기존 전투를 감싸는 구조와 최종 승인 책임은 이천서 기준으로 기록했다. HONG의 실제 기능 구현 기여는 아직 없다.

## 5. RFM01 GameScene 상점 규칙 선반영

### 5.1 수행 내용

- 카드 구매는 구매한 슬롯만 `SOLD OUT`으로 막고 방문 전체 구매 플래그를 두지 않아, 골드가 충분하면 남은 슬롯을 모두 살 수 있게 했다.
- 라이터와 위스키에 서로 독립된 방문당 1회 플래그를 두어 같은 상점에서 둘 다 이용할 수 있게 했다.
- 둘 중 하나 이상 이용한 방문을 닫을 때 가격 단계를 정확히 1 올리고 다음 방문의 두 서비스 가격에 함께 적용했다. 둘 다 이용해도 한 단계만 오르며, 미이용 방문은 유지한다.
- 새 런 재시작은 골드·상점 제안 순번·가격 단계·방문 플래그를 모두 초기화한다.
- 일반 상점과 라이터 카드 선택 화면에 `나가기` 버튼을 연결했다.
- GameScene MVP의 두 서비스 기본 가격 2골드와 이용 방문당 +1골드는 Inspector에서 조정 가능한 임시값으로 두었다.

### 5.2 변경 파일

- `Assets/01. Scripts/GameScene/ShopController.cs`
- `Assets/01. Scripts/GameScene/GameManager.cs`
- `Assets/06.Packages/Tests/EditMode/CoreLoop/ShopControllerTests.cs`
- 상점·런 관련 기획 문서와 공통 AI·역할 기록

### 5.3 검증

- RFM01 대상 3/3 통과: Unity MCP job `be7463c015f64188a8d6dbccba9526e6`
- 전체 EditMode 626/626 통과: Unity MCP job `1144276f15a64abd90841ce310416ab9`
- GameScene에서 첫 방문 위스키 이용 뒤 다음 방문의 라이터·위스키가 모두 2→3골드가 된 상태를 확인했다.
- 일반 상점의 `나가기`, 라이터 선택 패널의 `CANCEL`·`나가기`, 라이터 호버 `PRICE 3 GOLD`를 실제 Game View에서 확인했다.
- Play Mode 종료 뒤 `GameScene.unity`가 비저장 변경 없이 `isDirty=false`인 것을 확인했다.
- Test Framework 결과 저장 안내 1건과 기존 `ShopItem_Lighter/Body` 음수 스케일 BoxCollider 오류 1건을 분리했다. 신규 스크립트 컴파일 오류와 테스트 실패는 0이다.
- 씬·프리팹·Packages·외부 코드·외부 에셋·오픈소스는 변경하지 않았다.

### 5.4 책임과 경계

최종 기획·코드·화면 승인 책임자는 이천서이며 AI는 구조 대조, 구현, 테스트와 문서 갱신을 보조했다. 이번 작업은 기존 GameScene 독립 상점 MVP의 규칙 보정이다. HONG 담당의 RF-01 골드 정산, RF-02 정식 상점 도메인과 RF-03~RF-05 정식 런 연결은 완료로 기록하지 않는다.

## 5.5 RF-01A 수행 기록

### RF-01A — 런 골드 상태

- 작업자: 이천서(AI 구현·검증·기록 보조)
- 날짜: 2026-07-30
- 변경 파일: `PlayerRunState.cs`, `RunSaveCapture.cs`, `RunRestoreFactory.cs`, StageProgression 테스트 3개
- 구현 내용: 기본 골드 0, 명시 초기 골드 생성 경로, `CurrentGold`, 내부 지급·소비, 음수·부족·오버플로 방어, 새 런 초기화, 양수 골드 저장 캡처·복원
- 직접 결정·해결한 문제: 복원된 현재 골드를 새 런 초기 골드와 분리해 이어하기 후 재시작하면 0으로 돌아가게 했다.
- 대상 테스트 결과: RF01A 7/7 통과. Unity 생성 응답 파일로 `Border`와 `DiaBlackJack.StageProgression.Tests.EditMode` 컴파일 성공
- 전체 회귀 결과: Unity MCP 도구가 없고 Editor 프로세스가 실행 중이라 전체 EditMode 미실행
- RF-01A 완료 당시 제외 범위: 프로필별 지급량·승리 중복 방지·상점·UI. 이 중 지급량·승리 중복 방지는 RF-01B에서 후속 완료했다.
- 이천서 통합 검토: 구현 범위와 다음 단계 승인

## 5.6 RF-01B 수행 기록

### RF-01B — 프로필별 승리 골드 정산

- 작업자: 이천서(AI 구현·검증·기록 보조)
- 날짜: 2026-07-30
- 변경 파일: `GoldRewardCatalog.cs`, `StageProgressionSession.cs`, `FormalRunGoldRewardTests.cs`, 정식 런·공통 기록 문서
- 구현 내용: 겁쟁이 3·총잡이 4·광신도 6·사기꾼 7·집행자 9·보스 15의 명시적 카탈로그, 빈/중복/0 이하 정의와 알 수 없는 키 실패, 종료된 승리 전투 참조별 한 번 지급
- 직접 결정·해결한 문제: RF-03 바깥 세션이 아직 없으므로 기존 전투 종료 동기화 경계와 `_processedBattle` 참조 방어에 정산을 먼저 배치했다. RF-03은 이 경계를 유지하고 `RewardSelection`만 우회한다. 프로필 키가 없는 구형 스테이지는 기존 회귀를 위해 0골드를 유지한다.
- AI 보조 범위: 문서·코드 경계 대조, 구현·테스트 작성, 실패 원인 분석, 컴파일·대상 검증과 기록
- 대상 테스트 결과: RF01B 7/7, RF-01A 포함 RF-01 전체 14/14 통과
- 컴파일 결과: Unity 생성 응답 파일을 사용한 `Border`, `DiaBlackJack.StageProgression.Tests.EditMode` 컴파일 성공
- 전체 회귀 결과: Unity MCP가 연결되지 않고 Editor가 실행 중이어서 전체 EditMode 미실행
- 실제 씬·해상도·Console 결과: 순수 규칙 단계이며 UI·씬 변경 없음, 미실행
- 제외·잔여 위험: 기존 `RewardSelection` 우회, 상점 도메인·UI·저장 체크포인트는 RF-02~04·SV-06 범위
- 이천서 통합 검토: 구현 범위와 RF-02 전환 승인

## 5.7 RF-02 수행 기록

### RF-02 — 상점 도메인

- 작업자: 이천서(AI 구현·검증·기록 보조)
- 날짜: 2026-07-30
- 변경 파일: `StageProgression/Shop` 3개 규칙 파일, `PlayerRunState.cs`, `FormalRunShopTests.cs`, RF·저장·자동 카드·공통 문서
- 구현 내용: 불변 일반/악마 카드 옵션과 3:2 제안, 프로젝트 결정적 RNG 기반 생성, 자동 카드 5종 포함, 엘리트 숫자 5 이상 가중치 2배, 슬롯별 복수 구매, 라이터 제거, 위스키 회복, 무료 나가기와 거래 결과
- 직접 결정·해결한 문제: 미정 경제 수치는 GameScene과 같은 카드 3·서비스 2·방문당 +1·회복 2를 생성자 기본값으로만 두었다. 두 서비스 이용 여부를 방문 단위로 분리하고 가격 단계 증가는 바깥 RF-03 세션이 `HasUsedAnyUtility`를 한 번 읽도록 했다.
- 기존 시스템 변경과 이유: `PlayerRunState`에 카드 추가 가능 여부, 최소 덱 1장을 지키는 제거 가능/실행 경계를 추가했다. 마지막 발급 ID는 줄이지 않아 제거 ID를 재사용하지 않는다.
- AI 보조 범위: 구조 대조, 구현·테스트 작성, 실패 원자성·결정성 검증, 문서화
- 대상 테스트 결과: RF02 11/11 통과
- 컴파일 결과: `Border`, `DiaBlackJack.StageProgression.Tests.EditMode` 컴파일 성공
- 전체 회귀 결과: Unity MCP 미연결·Editor 실행 상태로 전체 Unity EditMode 미실행
- 실제 씬·해상도·Console 결과: UI·씬·프리팹을 변경하지 않은 순수 도메인 단계라 미실행
- 제외·잔여 위험: 상점 제안 보존, 두 방문 가격 단계 소유, `RewardSelection` 우회와 보스 즉시 승리는 RF-03; 화면은 RF-04
- 이천서 통합 검토: RF-02 구현 범위와 RF-03 전환 승인

## 5.8 RF-03 수행 기록

### RF-03 — 정식 순서 통합

- 작업자: 이천서(AI 구현·검증·기록 보조)
- 날짜: 2026-07-30
- 변경 파일: `RunProgress.cs`, `StageProgressionSession.cs`, `StageProgression/RunFlow/FormalRunSession.cs`, 상점 생성기·방문, `FormalRunSessionTests.cs`, RF·공통 기록 문서
- 구현 내용: 기존 RW 기본 동작 보존, 정식 세션 전용 무카드보상 승리 완료, 전투 결과 자동 동기화, 일반전 뒤 상점 0·1, 나가기 뒤 상대 선택·고정 보스, 보스 즉시 승리, 패배·재시작 초기화
- 직접 결정·해결한 문제: UI가 내부 `CombatSession`을 직접 사용해도 바깥 진행이 누락되지 않도록 내부 전투 결과 알림으로 연결했다. 다음 전투 준비가 성공하기 전에는 상점을 닫거나 방문 수를 늘리지 않는다.
- AI 보조 범위: 기존 세션·명세 대조, 코드·테스트 초안, 실패 분석, 컴파일·대상 검증과 문서화
- 대상 테스트 결과: RF03 12건, RF-01~03 누적 37/37 통과
- 컴파일 결과: Unity 생성 응답 파일 기반 `Border`, `DiaBlackJack.StageProgression.Tests.EditMode` 컴파일 성공
- 전체 회귀·화면 결과: Unity MCP 미연결·Editor 실행 상태로 전체 EditMode와 실제 씬 미실행. 순수 규칙 단계로 씬·프리팹·Packages·외부 에셋·새 의존성 변경 없음
- 제외·잔여 위험: Runtime·Presenter·View·Controller와 씬 왕복은 RF-04, 반복 전체 회귀와 실제 화면은 RF-05
- 이천서 통합 검토: RF-03 구현 범위와 RF-04 전환 승인

## 5.9 RF-04 수행 기록

### RF-04 — 진행 화면·씬 연결

- 작업자: 이천서(AI 구현·검증·기록 보조)
- 날짜: 2026-07-30
- 변경 파일: `StageProgressionRuntime.cs`, `RunSaveFlow.cs`, `RunRestoreFactory.cs`, `FormalRunSession.cs`, `StageProgressionPresentation.cs`, 진행 View·Controller, `CoreLoopController.cs`, `FormalRunPresentationTests.cs`, 관련 문서
- 구현 내용: 현재 저장 세션과 정식 세션 재결합, 신규·복원 세션 RW 우회, 전투 씬에 내부 세션 제공, 골드·예상 지급·상점 5장·판매 완료·라이터·위스키·가격 단계·나가기 표시와 입력
- 직접 결정·해결한 문제: SaveFlow가 새 게임·이어하기에서 세션을 교체하므로 Runtime이 참조 동일성을 확인해 정식 세션을 교체한다. 유산 세션의 `UsesBattleRewards`가 true이면 기존 보상 UI를 유지한다.
- AI 보조 범위: Runtime/저장/두 Controller 구조 대조, 구현·테스트 초안, 외부 실행 한계 분석, 문서화
- GameScene 후속 연결: `GameManager`가 정식 런의 실제 전투 세션·선택 상대 프로필·골드를 사용하고, 모든 행동을 진행 세션으로 전달하며, 종료 뒤 독립 상점 대신 진행 씬의 정식 상점으로 복귀하도록 했다. `StageTest.battleSceneName`을 `GameScene`으로 바꾸고 Build Settings에 GameScene을 추가했다.
- 검증: `Border`와 StageProgression 테스트 어셈블리 컴파일 성공, RF04 6건 중 비Unity 5건 실행 통과, RF-01~04 비Unity 누적 42/42, `git diff --check`, 씬 이름·GUID와 Build Settings 직렬화 대조
- 미실행: Unity 전용 Controller 테스트 1건, 전체 EditMode, 1280×720·1920×1080, 실제 `StageTest ↔ GameScene`, Console 실시간 확인. 등록된 `127.0.0.1:8080/mcp`는 TCP 연결되지만 initialize handshake가 실패하고 현재 작업에 Unity 호출 도구가 없음
- 변경 범위: `StageTest.unity`의 전투 씬 문자열, Build Settings, GameScene `GameManager` 코드. GameScene 자체 YAML·프리팹·Packages·외부 에셋·오픈소스·새 의존성은 변경하지 않았다.
- 이천서 통합 검토: 코드 연결 완료, 실화면 검증 뒤 RF-05 전환

## 5.10 RF-05 수행 기록

### RF-05 — 반복 검증과 마감

- 작업자: 이천서(AI 구현·검증·기록 보조)
- 날짜: 2026-07-30
- 변경 파일: `FormalRunSystemValidationTests.cs`와 `.meta`, 정식 런 문서·AI 활용·팀 역할·구조 기록
- 반복 검증: 전체 승리·재시작 10회, 구매·제거·회복·나가기 10회, 오래된 상대·상점 입력 10회, 첫째·둘째·보스 패배와 재시작 각 10회
- 프로필 검증: 겁쟁이 도박사·총잡이·광신도·사기꾼을 실제 상대 선택 후보에서 확정하고 3·4·6·7골드 정산 확인
- 상태 격리: 중복 카드 구매·제거·회복·나가기, 잘못된 상대·상점 ID, 정식 런 카드 보상 입력이 상태를 바꾸지 않음
- 씬 검증: `StageTest`와 `GameScene` 활성 Build Settings, `battleSceneName=GameScene`, Runtime·GameManager·ShopController 존재, 두 씬 누락 스크립트 0
- 테스트: RF-01~05 Unity 비의존 대상 49/49, 신규 RF05 8/8, Unity 6000.3.10f1 격리 복제본 전체 EditMode 744/744, 실패·건너뜀·미결정 0
- Console: 전체 EditMode 로그에서 `error CS`, 컴파일 실패, 처리되지 않은 예외·AssertionException 0
- 미검증: 실제 `StageTest ↔ GameScene` Play Mode, 1280×720·1920×1080 시각 배치와 실시간 Console. Unity MCP 8080 TCP 연결 후 initialize handshake 실패
- 외부 변경: 외부 에셋·외부 코드·오픈소스·새 패키지·새 의존성 없음
- 이천서 통합 검토: 자동 회귀 게이트 승인, 라이브 화면 게이트는 미완료로 유지

## 5.11 RFM02 수행 기록

### RFM02 — 시작 악마·상대 선택의 실제 게임 진입 연결

- 작업자: 이천서(AI 구조 대조·구현·검증·기록 보조)
- 날짜: 2026-07-30
- 변경 파일: `StageProgressionRuntime.cs`, `GameManager.cs`, `FormalRunSystemValidationTests.cs`, 정식 런 문서와 공통 기록
- 구현 내용: 프로토타입 플레이어의 기본 악마 4장을 제거하고 무작위 후보 2장 중 1장을 선택하게 했다. 선택 저장 뒤 첫 일반 스테이지의 상대 후보 2명을 제시한다. 활성 정식 전투 없이 GameScene을 실행하면 StageTest로 이동한다.
- 디버그 경계: 고정 총잡이·독립 상점 반복은 `allowStandaloneBattleForDebug`를 켠 경우에만 유지한다.
- 대상 테스트: `FormalRunSystemValidationTests` 13/13. 신규 RFM02는 매개변수 사례 포함 5/5.
- 회귀: StageProgression 전체 247/247. 두 어셈블리 778건 중 776건 통과; 기존 `DC08_I02` 바알제붑 선택 한도 정체와 `GSH01_U06` HUD 브러시 기대값 불일치 2건은 이번 변경과 무관한 별도 결함으로 재현했다.
- 실제 씬: Unity 6000.3.10f1에서 GameScene 직접 Play 시 StageTest로 전환됨. 전환 전후 Console Error 0.
- 외부 변경: 씬·프리팹·외부 에셋·오픈소스·패키지 추가 없음.
- 이천서 통합 검토: 실제 진입 연결 완료. 시작 악마는 2장을 모두 받는 방식이 아니라 기존 확정 규칙대로 2장 중 1장을 선택한다.

## 6. 구현 완료 기록 양식

```text
### RF-0N — 작업명

- 작업자:
- 날짜:
- 변경 파일:
- 구현 내용:
- 직접 결정·해결한 문제:
- 기존 시스템 변경과 이유:
- AI 보조 범위:
- 대상 테스트 결과:
- 전체 회귀 결과:
- 실제 씬·해상도·Console 결과:
- 제외·잔여 위험:
- 이천서 통합 검토:
```

## 7. 인수인계 핵심 계약

1. `StageProgressionSession`은 상대 선택·전투·전투 스테이지를 계속 소유하고 기존 카드 보상 코드는 유산 기능으로 보존한다.
2. 정식 런 UI는 기존 카드 보상 API를 표시하거나 호출하지 않고 바깥 세션의 승리 정산을 사용한다.
3. 종료 승리 전투 참조는 카드 보상 선택·건너뛰기와 무관하게 골드를 한 번 지급한다.
4. 일반전 승리·골드 정산 뒤에는 별도 카드 보상 없이 상점을 연다.
5. 상점에서 나간 뒤에만 `TryAdvanceToNextStage()`를 호출한다.
6. 첫 상점 뒤에는 상대 선택, 둘째 상점 뒤에는 고정 보스다.
7. 보스 골드는 기록하지만 상점을 열지 않는다.
8. 상점 변경 영혼·덱은 다음 전투 생성 전에 반영한다.
9. 카드 강화·유물·무작위 사건은 RF에서 선점하지 않는다.
10. 카드 구매는 슬롯별 재고만 제한하고 방문 전체 구매 상한을 두지 않는다.
11. 라이터·위스키는 방문당 각각 1회이며 둘 중 하나 이상 이용한 방문은 다음 상점의 두 가격을 한 단계만 올린다.
12. `나가기`는 무료이고 라이터 선택 상태에서도 사용할 수 있다.

## 8. 위험과 대응

| 위험 | 대응 |
| --- | --- |
| 이전 랜덤 이벤트 기획을 구현 | 첫 개발은 상점 고정이라는 최신 규칙을 테스트 이름과 문서에 명시 |
| 기존 보상 API 직접 호출로 골드 누락 | 정식 UI를 `FormalRunSession`으로 라우팅 |
| 상점 전 다음 전투 생성 | 나가기 승인 뒤에만 기존 다음 단계 API 호출 |
| 비용만 빠지고 효과 실패 | 모든 조건 사전 검증 뒤 비용·효과·플래그 일괄 적용 |
| 화면 재진입으로 상품 재추첨 | `ShopVisit`과 불변 Offer 보존 |
| 제거한 카드 ID 재사용 | 기존 단조 증가 ID를 줄이지 않음 |
| 담당 파일 충돌 | 새 RunFlow·Shop 폴더 우선, 기존 UI 최소 변경, 이천서 검토 |
| 서비스 둘 사용 시 가격 2단계 상승 | 방문별 공통 불리언 하나로 다음 가격 단계를 1회만 증가 |
| 카드 첫 구매 뒤 나머지 슬롯 차단 | 구매한 `OptionId`만 판매 완료로 기록하고 방문 전체 플래그 금지 |

## 9. 다음 작업

다음 작업은 **RF-05 Unity 실화면 마감**이다. MCP 연결을 복구한 뒤 1280×720·1920×1080에서 `StageTest ↔ GameScene`, 상대별 전투·골드·상점·보스·재시작과 실시간 Console을 확인한다. 자동 회귀는 완료됐다.

## 10. 변경 기록

| 날짜 | 작성자 | 변경 내용 |
| --- | --- | --- |
| 2026-07-30 | 이천서 | RFM02에서 시작 악마 2장 중 1장 선택과 일반전 상대 2명 중 1명 선택을 실제 Runtime에 연결하고, GameScene 직접 실행을 StageTest 정식 런으로 전환했다. 대상 13/13·StageProgression 247/247·실제 씬 전환·Console 0을 확인했다. |
| 2026-07-30 | 이천서 | RF-05 반복 검증 8건을 추가해 전체 승리·상점 행동·오래된 입력·세 위치 패배·네 일반 적 골드와 두 씬 직렬화를 검증했다. RF 대상 49/49·전체 EditMode 744/744 통과, 라이브 두 해상도는 MCP handshake 장애로 분리 기록했다. |
| 2026-07-30 | 이천서 | RF-04 후속으로 정식 전투 목적지를 GameScene으로 전환하고 GameManager의 진행 세션 행동·프로필·골드·복귀 경계를 연결했다. 컴파일과 RF 비Unity 42/42를 재확인했으며 라이브 씬 왕복은 MCP 연결 문제로 남겼다. |
| 2026-07-30 | 이천서 | RF-04 정식 Runtime 재결합과 진행 Presenter·IMGUI View·Controller·CoreLoop 씬 연결을 구현했다. RF04 6건 컴파일·비Unity 5건·누적 42/42와 기존 씬 직렬화를 확인했지만 Unity MCP가 닫힌 8080을 가리켜 실제 EditMode·두 해상도·씬 왕복은 미실행으로 남겼다. |
| 2026-07-30 | 이천서 | RF-03 무카드보상 정식 세션과 전투 결과 자동 동기화를 구현해 `전투→상점→전투→상점→보스`를 연결했다. RF03 12건·RF-01~03 누적 37/37·두 어셈블리 컴파일을 확인했고 전체 Unity EditMode·씬은 미실행으로 기록해 RF-04로 전환했다. |
| 2026-07-30 | 이천서 | RF-02 순수 상점 도메인과 최소 덱 제거 경계를 구현하고 대상 11/11·두 어셈블리 컴파일을 확인했다. 전체 Unity EditMode·씬은 MCP 미연결로 미실행이며 RF-03을 다음 작업으로 전환했다. |
| 2026-07-30 | 이천서 | RF-01B의 여섯 프로필 골드 카탈로그와 종료 승리 전투 참조별 1회 정산을 구현하고 RF01B 7/7·RF-01 전체 14/14·두 어셈블리 컴파일을 확인했다. 전체 Unity EditMode는 MCP 미연결·Editor 실행으로 미실행이며 RF-02를 다음 작업으로 전환했다. |
| 2026-07-30 | 이천서 | RF-01A 런 골드 보유·지급·소비·재시작과 저장 캡처·복원을 구현해 대상 7/7 및 두 어셈블리 컴파일을 확인했다. 전체 Unity EditMode는 MCP 부재·Editor 실행 중이라 미실행으로 남기고 RF-01B를 다음 단계로 전환했다. |
| 2026-07-30 | 이천서 | 무료 전투 카드 보상과 상점의 중복을 제거하는 기획을 문서에 반영했다. 자동 발동 카드 5종을 상점 일반 풀에 포함하고 일반전은 골드→상점, 보스는 골드 결과→즉시 승리로 변경했으며, 기존 RW 코드는 재사용 후보로 보존한다. 코드·테스트·씬은 변경하지 않았고 구현 이관은 미완료다. |
| 2026-07-29 | 이천서 | 적 6종 지급량을 3·4·6·7·9·15로 확정하고 RF-01B를 RF-01A 뒤 착수 가능한 상태로 전환; HONG 코드·테스트는 미착수 유지 |
| 2026-07-29 | 이천서 | 적 6종 재구성으로 RF-00 골드 수치를 과거 임시값으로 전환하고 RF-01A/01B 착수 경계를 기록; HONG 정식 RF 코드·테스트 미착수 유지 |
| 2026-07-29 | 이천서 | RFM01 GameScene 상점의 복수 카드 구매·라이터/위스키 독립 1회·이용 방문당 다음 가격 공통 +1·두 나가기 화면을 구현하고 전용 3/3·전체 626/626·실제 화면으로 검증; 정식 RF는 미완료로 분리 |
| 2026-07-20 | 이천서 | 최신 골드·상점 규칙으로 RF-00 인수인계 문서, 기준선, 다음 단계와 기록 양식 확정 |

