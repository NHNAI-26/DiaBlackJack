# 악마 계약 시스템 구현 계획서

> 프로젝트: DiaBlackJack  
> 계획·구현 책임자: 이천서  
> 작업 식별자: DC-00~DC-08
> 버전: v1.22
> 현재 단계: EPR07 광신도 2종 정책 이관 완료
> 다음 단계: 제외 악마 참조와 전체 콘텐츠 필터 재설계
> 최종 갱신: 2026-07-31

> **현행 콘텐츠 변경 안내 (2026-07-30)**
> 실제 프로토타입에는 사탄·벨페고르·바알제붑·아스모데우스·마몬·바포메트·아자젤만 등장한다. 시작 선택은 사탄·벨페고르·바알제붑·마몬 풀의 서로 다른 2장 중 1장이다. 아래 루시퍼·레비아탄·메피스토펠레스·파이몬·벨리알 구현 단계와 테스트 수치는 완료 당시의 증거로 보존하지만 출현 허용을 뜻하지 않는다.

> **광신도 정책 이관 결과 (2026-07-31):** 광신도 후보를 벨페고르·바알제붑 2종으로 제한하고 후보 수를 2장으로 변경했다. 상대 공개 카드 수만 보며 2장 이상이면 바알제붑, 아니면 벨페고르를 선택한다. 기존 영혼·사용 가능 여부·벨리알 분기를 제거했고 대상 21/21·전체 EditMode 793/793을 통과했다.

> **현행 기획 변경 안내 (2026-07-26)**
> 아래 DC-01~DC-08의 4장 시작 덱과 후보 3장 단계는 완료된 구현 이력으로 보존한다. DC-R01에서 새 런 악마 2장 중 1장 선택·최소 1장 덱, 일반 계약 최대 2장·단일 후보와 루시퍼 5장 경계의 순수 도메인·저장 복원을 적용했다. 실제 시작 UI와 기본 런 생성 경로는 빈 덱·선택 생성기·선택 직후 체크포인트로 연결되어 있다.

> **악마별 개정 안내 (2026-07-27)**
> 사탄 카운트 4·활성 계약 윗면/아랫면 상태, 마몬 행동 소비 재굴림, 레비아탄 리볼버 2회와 메피스토펠레스 보위 나이프 강화가 현행 기획이다. 사탄은 일반 카드의 앞면/뒷면을 바꾸지 않는다. DC-04·DC-06 완료 기록은 과거 구현 증거로 보존하며, 아래 DC-R 계열을 통과하기 전에는 개정 구현 완료로 표시하지 않는다.

> **레비아탄 현행 정정 (2026-07-29)**
> CU-M07에서 첫 실패에만 두 번째 예측을 제공하고 두 번 모두 실패할 때만 영혼 1을 잃도록 정정했다. 아래 DC-R03의 무조건 두 번째 발동·최종 생존 대가는 과거 구현 기록이다.

> **Notion v0.7 적 계약 재명세 (2026-07-29)**
> 기존 완료 단계는 실제 검증 이력으로 유지한다. EP-R04에서 아스모데우스 7 이하 제한을, EP-R05에서 광신도의 성공 전 매 차례 계약과 집행자 파이몬을, EP-R06에서 보스의 시작·영혼 5·2 계약 단계를 이관했다. DC-R06 적별 계약 범위는 완료되었다.

## 1. 계획 목적

악마 계약 시스템을 데이터·덱, 계약 행동, 첫 수직 악마, 나머지 효과, 화면·런, 적 AI와 최종 검증으로 나눠 구현한다. 각 단계는 문서 확인 → 회귀 테스트 또는 새 테스트 작성 → 최소 구현 → 대상·전체 검증 → 기록 → 커밋 순서로 독립 완료한다.

한 단계의 완료 증거가 없으면 다음 단계의 코드를 미리 합치지 않는다. 사탄은 규칙 충돌이 있으므로 결정 게이트를 통과하기 전 구현하지 않는다.

## 2. 실행 원칙

1. 단계별로 작은 커밋을 만든다.
2. 행동 변경 전 기존 회귀 테스트로 현재 규칙을 고정한다.
3. 순수 C# 규칙과 테스트를 UI보다 먼저 구현한다.
4. 조회·표시 경계는 상태를 변경하지 않는다.
5. 난수와 셔플은 결정적으로 주입해 재현한다.
6. 일반 카드 효과를 복제하지 않고 필요한 계약 훅만 연결한다.
7. 단계 완료 뒤 AI 활용·팀 역할·진행 기록을 함께 갱신한다.
8. AI는 사용자의 명시적 요청 없이 스테이징·커밋하지 않는다.

## 3. 단계 요약

| 단계 | 목표 | 주요 산출물 | 상태 |
| --- | --- | --- | --- |
| DC-00 | 기획·개발 기준과 결정 게이트 | 문서 4종·색인·공통 기록 | 완료 |
| DC-01 | 계약 정의와 런·전투 악마 덱 | 카탈로그·물리 ID·덱·변환 | 완료 |
| DC-02 | 비용·횟수·후보 3장·선택 상태 | 전투 API·보류 상호작용·세션 전달 | 완료 |
| DC-03 | 벨페고르 수직 기능 | 히트 미리보기·덱 아래·자동 스탠드 | 완료 |
| DC-04 | 마몬·레비아탄 | 주사위 선택·리볼버 후속 효과·대가 | 완료 |
| DC-05 | 플레이어 계약 화면과 런 연결 | 표시 모델·입력·지속 영혼·두 해상도 | 완료 |
| DC-06 | 사탄 결정·구현 | 카운터·버스트 방지·권능 카드 | 완료 |
| DC-07 | 적 AI 계약과 최종 검증 | 광신도 정책·반복 회귀·문서 마감 | 완료 |
| DC-08 | 광신도 계약 밸런스·실전 검증 | 효용 조건·400시드 분포·100자동전투 | 완료 |
| DC-R06 | Notion v0.7 적별 계약 이관 | 아스모데우스·광신도·집행자·보스 시작/영혼 5·2 전이 | 완료, EP-R06 전용 10/10·전체 665/665 |
| DC-V01 | GameScene 일반 계약 월드 후보 | 즉시 계약·하단 반노출 1~2장·호버 상세·모달 입력 | 완료, 대상 10/10·720p/1080p; 전체 회귀 1건 별도 실패 |

## 4. DC-00 — 문서 기준 수립

### 작업

- `rule.md`, 전체 기획과 현재 카드·전투·런·AI 구조를 대조한다.
- 계약 비용, 후보, 횟수, 수명과 행동 소비를 명시한다.
- 시작 악마 덱과 우선 악마 4종의 프로토타입 규칙을 정한다.
- 사탄·레비아탄 등 충돌하거나 사람이 확인할 항목을 결정 대장으로 분리한다.
- 기획서·개발 명세서·구현 계획서·진행 기록서를 작성한다.
- README, AI 활용과 이천서 역할 기록을 갱신한다.

### 완료 게이트

- 네 문서의 작업 ID, 책임자, 현재·다음 단계가 일치한다.
- `현재 영혼 > 비용`, 전투당 1회, 위 3장 중 1장 선택이 모두 명시된다.
- 구현하지 않은 기능은 완료로 기록되지 않는다.
- 코드·테스트·씬·패키지·외부 에셋 변경이 없다.

### 완료 결과

- DC-00 문서 4종과 문서 색인을 수립했다.
- 카드 사용 문서 상태를 실제 완료된 CU-M03으로 정리했다.
- 최근 자동 검증 기준선 310/310을 과거 증거로만 기록하고 Unity를 재실행하지 않았다.
- 이천서를 기획·개발 책임자로, AI를 구조 대조·초안 보조로 기록했다.

### 권장 커밋 제목

`계약 개발이 충돌 없는 기준에서 시작되도록 규칙과 경계를 고정하다`

## 5. DC-01 — 계약 데이터와 런·전투 덱 기반

### 목표

화면과 효과 없이도 네 악마를 안정된 키와 물리 카드로 보존하고, 런 덱에서 전투마다 독립적인 계약 덱을 만들 수 있게 한다.

### 작업

- `DemonContractDefinition`, 종류와 카탈로그
- 사탄·벨페고르·마몬·레비아탄 정의
- `RunDemonDefinition`과 플레이어 최초·현재 악마 덱
- 전투용 `DemonContractCard`, 드로우·버림 덱
- 후보 3장 확보와 버린 더미 보충 명령
- 일반 카드와 분리된 `StageBattleFactory` 변환
- 셔플 시드 독립과 전투·재시작 상태 격리
- 정의·덱·카드 총수·변환 단위 테스트

### 완료 게이트

- 네 정의 키, 표시명과 기본 비용이 카탈로그에서 조회된다.
- 알 수 없는 키와 중복 물리 ID를 거절한다.
- 후보 이동 전후 카드 총수와 ID가 보존된다.
- 일반 카드 덱 순서는 악마 덱 셔플 때문에 바뀌지 않는다.
- 전투마다 새 악마 덱을 사용하고 런 재시작 시 초기 4장이 복구된다.
- 대상 테스트와 전체 EditMode가 통과한다.

### 권장 커밋 제목

`악마 카드가 일반 덱을 침범하지 않고 런과 전투 사이에서 보존되게 하다`

### 완료 기록

- 네 계약 정의와 카탈로그, 런 물리 카드, 전투 카드·드로우·버림 덱을 구현했다.
- 기본 `PlayerRunState`는 사탄·벨페고르·마몬·레비아탄 각 1장의 최초/현재 덱을 분리해 보존한다.
- `StageBattleFactory`는 일반 카드와 별도로 악마 덱을 변환하고, 전투마다 새 덱과 독립 시드를 사용한다.
- 후보 3장, 부족 시 무변경 거절, 버린 더미 보충, 활성 카드 제외와 물리 ID 총수 보존을 테스트했다.
- DC-01 대상 19/19, 전체 EditMode 329/329와 컴파일 오류 0을 확인했다. 테스트 뒤 Console에는 Unity Test Framework 기반 시설 경고 4건과 결과 저장 `Exception` 2건만 남았으며 게임 코드 오류와 구분했다.
- 계약 비용·횟수·선택 상태·개별 효과·UI는 계획대로 DC-02 이후에 남겼다.

## 6. DC-02 — 계약 행동과 후보 선택

### 목표

영혼 비용과 전투당 제한을 지키며 후보 3장 중 하나를 원자적으로 활성화하는 공통 계약 행동을 완성한다.

### 작업

- 계약 가능 여부·불가 사유·계약 후 영혼 모델
- `PlayerResolvingDemonContract` 상태
- 보류 상호작용·옵션·증가형 `InteractionId`
- `TryBeginPlayerDemonContract`
- `TryResolvePlayerDemonContract`
- 비용 1, `현재 영혼 > 비용`, 전투당 1회
- 선택 1장 활성, 미선택 2장 버림
- 계약 결과와 차례 소비
- 개별 대가 영혼 0 즉시 패배와 후속 처리 중단
- 동일 악마 추가 계약을 위한 물리 인스턴스 분리
- CoreLoopSession·StageProgressionSession 전달
- 정상·거절·오래된 입력·중복 입력 테스트

### 완료 게이트

- 영혼 1에서는 무변경 거절하고 영혼 2에서는 1을 지불해 후보를 연다.
- 후보 공개 뒤 취소, 히트·스탠드·체인지·카드·추가 계약을 거절한다.
- 잘못된 상호작용 ID와 옵션은 카드 위치·영혼·차례를 바꾸지 않는다.
- 정확히 한 장만 활성화되고 두 장은 버린다.
- 두 번째 기본 계약을 거절한다.
- 효과가 없는 테스트 처리기로 행동이 적 차례까지 정확히 한 번 이어진다.
- 같은 악마 계약 두 개가 서로 다른 ID·런타임 상태를 유지한다.
- 개별 대가로 영혼이 0이 되면 버스트 처리 없이 즉시 전투가 끝난다.
- 대상·전체 EditMode가 통과한다.

### 권장 커밋 제목

`영혼을 건 계약 선택이 취소 악용과 중복 입력 없이 한 번만 성립하게 하다`

### 완료 기록

- `DemonContractAvailability`가 비용 1, 계약 후 영혼, 남은 기본 횟수와 상태별 불가 사유를 무변경 조회한다.
- `PlayerResolvingDemonContract`, 세 옵션의 `PendingDemonContractInteraction`과 증가형 `InteractionId`를 추가했다.
- 시작 전 후보 수를 확인하고, 승인 시 영혼 1·기본 횟수 1을 적용한 뒤 후보 3장을 보류 위치로 이동한다.
- 올바른 ID와 옵션만 선택 카드 하나를 `ActiveDemonContract`로 등록하고 나머지 둘을 버리며, 중복·오래된 입력은 무변경 거절한다.
- `DemonContractResolver`는 기본 악마 효과를 등록하지 않은 채 주입식 활성화 경계만 제공한다. 이를 통해 개별 대가 영혼 0이 버스트 없이 즉시 패배하고 적 AI가 실행되지 않는 경로를 검증했다.
- `CoreLoopSession`과 `StageProgressionSession`이 두 계약 입력을 전달하고, 계약 대가 패배의 지속 영혼·런 패배를 한 번만 동기화한다.
- DC-02 대상 13/13과 전체 EditMode 342/342, 컴파일 오류 0을 확인했다. UI·Game View는 DC-05 범위이며 씬·프리팹·패키지·외부 에셋을 변경하지 않았다.

## 7. DC-03 — 벨페고르 수직 기능

### 목표

계약 공통 기반이 실제 지속 효과, 전용 선택, 덱 이동과 차례 제약까지 연결되는 첫 세로 기능을 완성한다.

### 작업

- 벨페고르 처리기와 활성 런타임 상태
- 히트 입력 전 덱 위 카드 미리보기
- 그대로 공개 히트 / 덱 아래 이동 후 차례 넘김
- 상대 스탠드 뒤 다음 플레이어 차례 종료 자동 스탠드
- 정보 은닉 표시 모델
- 카드 부족·버스트·전투 종료 중 후속 처리 차단
- 독립·런 전투 통합 테스트

### 완료 게이트

- 미리보기 카드와 실제 히트·덱 아래 이동 카드가 같은 물리 ID다.
- 미리보기 값이 적 관측과 공용 로그에 노출되지 않는다.
- 히트 외 행동은 미리보기를 발동시키지 않는다.
- 자동 스탠드가 한 번만 적용되고 전투 종료 뒤 남지 않는다.
- 공개 합 중간 버스트와 전체 합 최종 승부 규칙이 유지된다.
- 대상·전체 EditMode가 통과한다.

### 권장 커밋 제목

`첫 계약이 덱 정보와 차례 제약을 숨은 정보 유출 없이 바꾸게 하다`

### 완료 기록

- 기본 Resolver에 확정된 벨페고르 처리기만 등록하고 물리 계약 인스턴스별 `BelphegorRuntimeState`를 생성했다.
- 히트 전에 실제 덱 위 카드를 이동 없이 확인하고, 공개 옵션과 분리된 `PlayerDemonContractPreview`에만 카드 정보를 보존한다.
- 그대로 히트와 덱 아래 이동은 미리 본 카드 ID를 다시 확인한다. 히트는 기존 공개 드로우·공개 합 버스트를 재사용하고 이동은 드로우 없이 차례를 종료한다.
- 상대 스탠드가 확인된 플레이어 정상 차례에 자동 스탠드를 예약하고, 카드·체인지·계약·벨페고르 선택을 포함한 행동 완료 뒤 한 번만 소비한다. 라운드 종료와 전투 종료에서는 예약을 초기화한다.
- 미리보기 전후 적 관측·공용 로그 불변, 카드 사용·체인지 비발동, 독립·런 전투를 DC-03 대상 9/9로 확인했다.
- Unity 6000.3.10f1에서 전체 EditMode 351/351과 컴파일 오류 0을 확인했다. `GameScene`·다른 UI·씬·프리팹·패키지·외부 에셋은 변경하지 않았다.

## 8. DC-04 — 마몬과 레비아탄

### 목표

난수·최종 합계 선택과 기존 카드 효과 후속 훅이라는 서로 다른 계약 확장 경계를 검증한다.

### 작업

- 주입 가능한 6면체 난수와 마몬 런타임 상태
- 계약 즉시 굴림, 차례 시작 재굴림, 6 즉시 버스트
- 최종 비교 직전 주사위 적용·미적용 선택
- 레비아탄의 리볼버 완료 후 추가 판정
- 추가 성공과 최종 실패 영혼 1 대가
- `ContractEffectBust` 종료 원인과 안전한 결과 모델
- 숨은 전체 합·주사위 미래값 미노출 테스트

### 완료 게이트

- 고정 난수 1~6이 모두 재현된다.
- 마몬 재굴림은 차례당 한 번이며 6은 즉시 라운드를 끝낸다.
- 마몬 값을 더하지 않은 경로와 더해 승리·버스트하는 경로가 구분된다.
- 리볼버 성공 시 레비아탄 대가가 없고, 추가 성공 시 상대만 버스트한다.
- 완전 실패 시 영혼 1을 정확히 한 번 잃고 고갈 시 전투가 끝난다.
- 상대 숨은 합이 결과·UI·AI 관측에 노출되지 않는다.
- 대상·전체 EditMode가 통과한다.

### 권장 커밋 제목

`계약의 난수와 카드 연계 위험이 같은 종료 규칙 아래 판정되게 하다`

### 완료 기록

- `IDemonDieRoller`와 물리 계약별 `MammonRuntimeState`를 추가해 계약 즉시 굴림, 정상 차례 전 유지·재굴림, 새 6의 계약 버스트를 재현 가능하게 만들었다.
- 양쪽 스탠드 뒤 `MammonApplyDie`를 보류하고, 적용하지 않음·적용 승리·적용 숫자 버스트를 기존 라운드 해석과 구분했다. 주사위는 중간 공개 합과 카드 객체를 바꾸지 않는다.
- `ContractEffectBust`를 숫자·카드 버스트와 분리하고 최종 보정 합이 에이스 가치까지 함께 재계산되도록 했다.
- 레비아탄은 실패한 리볼버 완료 결과만 후처리한다. 원 효과 성공은 그대로 종료하고, 내부 전체 합 22 이상은 상대 계약 버스트, 그 외는 플레이어 영혼 1 대가와 영혼 0 즉시 패배로 연결했다.
- 안전한 결과 모델은 발동 여부·버스트 대상·지불 영혼만 공개하고 상대 전체 합과 비공개 숫자를 노출하지 않는다.
- DC-04 대상 11/11과 Unity 전체 EditMode 362/362, 최종 컴파일 오류 0을 확인했다. 현재 Codex 세션의 Unity MCP 도구가 노출되지 않아 열린 Editor의 공식 Test Runner API를 사용했으며 씬·UI·프리팹·패키지·외부 에셋은 변경하지 않았다.

## 9. DC-05 — 플레이어 계약 화면과 런 연결

### 목표

플레이어가 실제 전투 화면에서 비용을 확인하고 계약·후속 선택을 완료하며 결과를 런까지 이어갈 수 있게 한다.

### 작업

- 계약 가능·불가 사유와 계약 후 영혼 표시
- 비용 지불 전 확인과 취소
- 후보 3장, 능력·대가·선택 입력
- 활성 계약, 주사위·카운터 자리와 최근 결과 표시
- 계약 선택 중 다른 입력 잠금
- Controller의 독립·런 세션 전달
- 계약 효과 승패·영혼·보상 동기화
- 사탄 권능·오망성 제거와 파이몬 추방·벨리알 소유권 원상복구 표시 결과
- GameScene 스크립트 표시 연결
- 두 해상도·두 씬·Console 검증

### 완료 게이트

- 비용 지불 전에는 취소할 수 있고 지불 뒤에는 후보 하나를 반드시 선택한다.
- 후보 3장의 능력과 대가가 잘리지 않고 구분된다.
- 선택 중 중복 클릭과 일반 행동이 실행되지 않는다.
- 독립·런 전투 결과와 지속 영혼이 일치한다.
- 양측 비공개 카드 최좌측 표시와 상대 정보 은닉이 유지된다.
- 계약 임시 카드는 라운드 전환 뒤 유지되고 전투 종료 뒤 런 덱에 남지 않는다.
- `GameScene.unity` 직렬화 변경 없이 우선 연결하며 필요 시 별도 시각 작업으로 기록한다.
- 대상·전체 EditMode, 1280×720·1920×1080, 두 씬과 Console이 통과한다.

### 완료 기록

- `DemonContractPanelViewModel`과 후보 표시 모델을 추가해 비용·계약 후 영혼·불가 사유, 후보 능력·대가, 활성 계약·최근 결과를 안전하게 투영했다.
- `CoreLoopView`와 `GameManager`에 지불 전 확인·취소, 후보·후속 선택, 입력 잠금과 계약 상태 패널을 연결했다. 독립 전투에는 네 장 프로토타입 악마 덱을 주입했고 런 전투는 기존 세션 전달·결과 동기화를 재사용했다.
- 사탄은 미확정 효과를 작동시키지 않도록 후보에서 `DC-06 구현 예정`으로 비활성화했다. 사탄 권능·임시 카드 표시의 실제 구현은 DC-06 이후 범위로 남겼다.
- DC-05 대상 테스트 7/7, 전체 EditMode 369/369, 컴파일 오류 0을 확인했다.
- `GameScene`·`CoreLoopTest`를 1280×720·1920×1080에서 확인해 후보 3장의 이름·능력·대가·선택 버튼이 잘리지 않고 양측 비공개 카드 정보가 유지됨을 검증했다.
- Unity MCP 표준 HTTP 세션 `DiaBlackJack@5635a4cdcfecc8dd`에서 테스트·Play Mode·Game View·Console을 검증했으며 `.unity` 씬·프리팹·Packages·외부 에셋은 변경하지 않았다.

### 권장 커밋 제목

`계약의 이득과 대가를 확인한 선택이 실제 런 결과까지 이어지게 하다`

## 10. DC-06 — 사탄 결정과 구현

### 착수 게이트 — 통과

- 카운터는 6에서 시작해 양측 정상 차례 시작마다 감소한다.
- 0에서 영혼 2를 잃고 계약을 종료하며 반복하지 않는다.
- 화염(10)·괴력(8)은 하나의 물리 양면 카드다.
- 뒤집힌 면은 같은 물리 ID와 사용 완료 상태를 유지한다.

### 작업

- 스탠드 불가와 표시 사유
- 숫자·카드·계약 버스트 방지
- 정상 차례 전용 카운터 감소/증가
- 영혼 2 대가와 전투 종료
- `사탄의 권능` 물리 카드·양면 효과·사용 상태
- 권능 숫자 변경 직후 합계 재계산과 전투 종료 제거
- 추가 행동·카드 효과·양측 스탠드 불가 교착 테스트

### 완료 게이트

- 확정된 카운터 문구와 코드·UI 값이 일치한다.
- 모든 버스트 원인에서 방지 여부와 카드 이동이 정의대로 처리된다.
- 영혼 대가가 정확한 시점에 한 번 적용된다.
- 사탄 때문에 라운드가 영구 교착되지 않는다.
- 권능 카드가 복제·유실되지 않고 계약 종료 규칙을 따른다.
- 권능 카드가 라운드 사이에는 유지되고 전투 종료 뒤 런 덱에는 남지 않는다.
- 대상·전체 EditMode와 실제 전투 화면이 통과한다.

### 완료 기록

- `SatanDemonContractHandler`가 계약별 카운터 6, 양측 정상 차례 시작 감소, 스탠드 금지, 모든 플레이어 버스트 방지와 0 도달 영혼 2 대가를 담당한다.
- `SatanPowerEffectHandler`와 임시 카드 API를 추가해 화염(10)의 조건부 강제 히트·비버스트 폐기, 괴력(8)의 서로 다른 숫자 두 개 선언과 단일 비공개 카드 판정을 구현했다.
- 권능은 같은 물리 ID로 면만 바꾸고 현재 라운드의 사용 완료 상태를 유지한다. 계약·전투 종료 때 손·덱·버린 카드 더미 어느 위치에 있든 제거한다.
- 계약 종료 직후 영혼 0이면 즉시 전투 패배, 생존했더라도 공개 합이 21을 넘으면 즉시 숫자 버스트로 처리한다.
- 사탄 상태·현재 권능 면을 계약 패널에 투영하고 `CanPlayerStand`를 공용 표시 경계로 연결했다. 씬 파일 수정 없이 두 기존 코드 UI에서 동작한다.
- DC-06 대상 테스트 8/8, 전체 EditMode 377/377, 컴파일 오류와 최종 Console 오류 0을 확인했다.
- `GameScene`·`CoreLoopTest`를 1280×720·1920×1080에서 확인했으며 최종 시각 판정 94점으로 통과했다. `.unity` 씬·프리팹·Packages·외부 에셋은 변경하지 않았다.

### 권장 커밋 제목

`사탄의 무적과 대가가 교착 없이 끝나는 하나의 규칙으로 작동하게 하다`

## 11. DC-07 — 적 AI 계약과 최종 검증

### 목표

광신도부터 공개 정보만으로 계약을 평가·선택하게 하고 플레이어 계약 전체를 반복 검증해 1차 작업을 마감한다.

### 작업

- 적 유효 행동 후보의 계약 종류
- 공개 정보 전용 계약 평가 입력
- 광신도 계약 가중치와 실행 직전 재검증
- 적 후보 선택·후속 선택 정책
- 네 악마 정상·거절·위험 경로 반복 테스트
- 동일 악마 중복 계약의 인스턴스·대가 격리 테스트
- 사탄 권능·오망성·파이몬 추방·벨리알 탈취의 전투 종료 원상복구 테스트
- 독립·런·상대 선택·보상·재시작 통합 회귀
- 카드 총수·상태 격리·난수 재현 50회
- 두 씬·두 해상도·Console 최종 검증
- 진행·AI 활용·팀 역할·프로젝트 구조 문서 마감

### 완료 게이트

- 적 AI가 플레이어 비공개 값, 덱 순서와 미래 난수를 읽지 않는다.
- 영혼·횟수·후보 조건이 플레이어와 같은 방식으로 재검증된다.
- 광신도가 계약을 사용할 수 있지만 무조건 자멸하지 않는다.
- 네 악마의 정상·위험·종료·재시작 경로가 각 10회 통과한다.
- 전체 EditMode, 실제 흐름, 화면과 Console에 알려진 오류가 없다.
- 이천서의 실제 구현 파일과 검증만 완료 기여로 기록된다.

### 완료 기록

- 계약 평가 단계에는 덱을 소비하지 않는 일반 후보만 제공하고, 실행 승인 뒤 영혼 비용·횟수·후보 수를 다시 검사한 다음 실제 후보 3장을 가져오도록 분리했다.
- `CultistEnemyPolicy`가 계약 시작과 네 악마 선택, 벨페고르 공개 합 판단, 마몬 재굴림·최종 적용을 수행한다. 계약 후 영혼이 사탄 종료 대가 이하라면 다른 악마를 우선해 무조건 자멸하지 않는다.
- 플레이어 전용이던 계약 처리 훅을 소유자 방향으로 일반화해 적 사탄·벨페고르·마몬·레비아탄에도 동일한 비용·버스트·대가·라운드·전투 종료 규칙을 적용했다.
- `StageBattleFactory`는 광신도에게만 전투별 독립 4장 악마 덱을 주입하며 다른 적은 빈 덱을 유지한다. 적 활성 계약은 기존 계약 패널에 `상대` 소유자로 표시하고 적 전용 미리보기는 노출하지 않는다.
- DC-07 대상 12/12, CoreLoop EditMode 260/260, 전체 EditMode 389/389를 통과했다. 네 악마별 10전 격리와 Cultist 덱 50회 생성·격리를 포함한다.
- `GameScene` Full HD Play Mode 스모크와 최종 Console Error/Warning 0을 확인했다. UI 배치와 씬 직렬화는 바꾸지 않았으므로 DC-05~06의 720p·1080p 레이아웃 기준을 유지했다.
- `GameScene.unity`·프리팹·Packages·외부 에셋·오픈소스는 변경하지 않았다.

### 권장 커밋 제목

`계약이 플레이어와 적의 공정한 선택으로 반복 전투에서도 안정되게 하다`

## 12. DC-08 — 광신도 계약 밸런스·실전 검증

### 목표

DC-07의 고정 악마 점수로 발생한 벨페고르 편향을 측정하고, 현재 전투 자원에서 실제 효용이 있는 계약만 결정적으로 분산 선택하게 한다.

### 완료 기록

- 변경 전 400시드에서 벨페고르가 310회 선택되는 77.5% 편향을 재현했다.
- 사탄은 종료 대가 이후 영혼이 남을 때, 레비아탄은 사용하지 않은 리볼버를 보유할 때만 우선하도록 했다.
- 현재 최대 영혼 3·리볼버 없는 광신도는 사탄·레비아탄을 고르지 않고, 벨페고르·마몬을 같은 점수와 결정적 후보 순서로 각각 40~60% 범위에 분산한다.
- 벨페고르 공개 합 21 경계, 마몬 2/3 재굴림 경계와 최종 전체 합 적용을 단위 테스트로 고정했다.
- 같은 시드 50회 재현과 100회 자동 전투의 200행동 내 종료를 확인했다.
- `CultistEnemyPolicy.cs`와 새 EditMode 테스트만 변경하며 씬·프리팹·Packages·외부 에셋은 변경하지 않는다.

### 완료 게이트

- DC-08 대상 8/8, CoreLoop 268/268, 전체 EditMode 397/397 통과
- `GameScene` Full HD Play Mode에서 계약 표시·입력 교착 없음
- 최종 Console Error/Warning 0과 같은 시드 선택 재현

### 권장 커밋 제목

`광신도의 계약 선택이 위험과 효용 사이에서 일관되게 작동하도록 조정하다`

## 13. DC-R00~DC-R05 — 노션 v0.6 개정 이관

| 단계 | 목표 | 주요 완료 게이트 | 상태 |
| --- | --- | --- | --- |
| DC-R00 | `rule.md`와 계약 문서 4종을 최신 노션 규칙으로 통일 | 새 규칙과 과거 구현 기록이 구분되고 이천서 책임·AI 보조가 기록됨 | 완료 |
| DC-R01 | 시작 악마 2장 중 1장 선택, 일반 계약 후보 1~2장과 루시퍼 5장 경계 | 0장 무변경 거절·1장 단일 선택·2장 선택/버림·시작 선택 저장 통과 | 완료 |
| DC-R02 | 사탄 카운트 4와 활성 계약 윗면/아랫면 효과 | 소유자 차례 시작 감소·종료 상태 전환·영혼 대가 1회·별도 권능 제거·양측 대칭 통과 | 완료 |
| DC-R03 | 마몬 행동 소비 재굴림과 레비아탄 리볼버 2회 | 재굴림 뒤 즉시 차례 종료·두 선언 정보 은닉·첫 전투 종료 중단·최종 생존 대가 통과 | 완료 |
| DC-R04 | 바알제붑·아스모데우스·루시퍼·메피스토펠레스·파이몬·벨리알·바포메트·아자젤 수직 구현 | 각 능력·대가·영혼 0·버스트·소유권·전투 종료 복구 전용 테스트 통과 | 완료 |
| DC-R05 | 시작 선택 런타임·활성 계약 입력·후보 레이아웃·전체 회귀 | 대상 4/4, CoreLoop 425/425, StageProgression 189/189, 저장 재진입, 두 화면 720p/1080p와 시각 판정 통과 | 완료 |
| CU-M07 | 레비아탄 첫 실패 조건부 재예측·GameScene 연출 | 첫 성공 종료·첫 실패 연출 후 재준비·두 번째 성공 무비용·두 번 실패 영혼 1, 대상 4/4·전체 631/631 | 완료 |

DC-R01은 이후 단계가 기대하는 후보 수와 시작 덱을 먼저 고정한다. DC-R02와 DC-R03은 같은 `CoreLoopBattle`·계약 상태를 변경하므로 순차 수행한다. DC-R04의 개별 악마는 서로 다른 파일과 전용 테스트를 소유할 때만 병렬화하고, 공용 전투 상태·Resolver·씬 파일은 동시에 수정하지 않는다.

### DC-R01 구현 범위

- `CandidateCount = 3`과 정확히 3장을 요구하는 전투 검사를 최대 2장 요청으로 교체
- 가용 카드 1장 계약과 0장 무변경 거절
- 런 시작 후보 2장 생성·1장 필수 선택·선택 직후 첫 체크포인트
- 루시퍼 전용 후보 5장 요청을 일반 계약 상수와 분리
- 기존 카드 총수·물리 ID·결정적 셔플·실패 원자성 회귀

### DC-R01 완료 기록

- 일반 계약은 가용 카드 중 최대 2장을 제안하고, 1장은 단일 선택·0장은 무변경 거절한다.
- 루시퍼의 5장 요구를 일반 상수·일반 요청과 분리한 내부 요청 경계로 만들었으며 루시퍼 개별 효과는 DC-R04에서 구현한다.
- `StartingDemonSelectionGenerator`와 세션 선택 API가 결정적 2장 제안·1장 선택·오래된 입력 거절을 담당한다.
- 선택한 한 장은 최초·현재 런 악마 덱, 재시작, `StartingDemonSelected` 체크포인트 캡처·복원에 보존된다.
- 실제 `StageProgressionRuntime`은 이미 빈 덱·시작 선택 생성기·`RunSaveFlow`를 사용한다. 네 장 프로토타입 덱은 생성기를 주입하지 않은 테스트·호환 경로에만 남긴다.
- 전용 14/14와 전체 EditMode 511/511를 Unity MCP에서 통과했다.

### DC-R02 완료 기록

- 활성 계약 인스턴스의 `SatanRuntimeState`에 종말 카운트 4, 윗면/아랫면과 대가 적용 여부를 저장했다.
- 소유자 정상 차례 시작에만 카운트를 줄이고 0 도달 시 영혼 2를 정확히 한 번 차감하며, 계약은 종료하지 않고 전투 끝까지 스탠드·버스트 제한을 유지한다.
- 계약 선택 차례에는 윗면을 유지하고 다음 소유자 정상 차례부터 종료마다 면을 뒤집는다.
- 윗면의 서로 다른 숫자 두 개 선언과 아랫면의 상대 강제 공개 히트·21 이하 신규 카드 폐기를 활성 계약 행동으로 구현했다.
- 아랫면 강제 히트로 자동 카드 선택이 열릴 때 계약 연속 처리를 저장하고, 선택 완료 뒤 정확히 한 번 재개하도록 했다.
- 플레이어·적 실행을 같은 소유자 방향 규칙으로 처리하고 광신도는 공개 숫자 추론만으로 윗면 선언을 고른다.
- 과거 숫자 8/10 사탄 권능 정의와 처리기를 제거했다. 저장된 enum 숫자 호환용 과거 슬롯 이름만 남기고 실행 등록은 하지 않는다.
- 세션 API와 표시 모델은 연결했지만 실제 `GameScene` 월드 계약 카드 클릭·호버·윗면/아랫면 상태 표시는 DC-R05 통합 범위로 남겼다.
- Unity MCP에서 최종 전용 12/12(job `a8941795cdc7467b9fd0f1556ec9980b`), CoreLoop 363/363(job `503571cfabf5471c924dc64a79a345d4`), 최종 전체 EditMode 553/553(job `68bd6a58eea7459e8a1df78391e6902a`)을 통과했다.

### DC-R03 구현 범위

- 마몬 재굴림을 차례 시작 상호작용이 아닌 정상 행동 후보로 노출
- 레비아탄이 기존 리볼버 숨은 값 비교를 두 번 안전하게 요청하도록 연속 처리
- 광신도 정책의 마몬 재굴림·레비아탄 효용을 최신 규칙으로 재산정

### DC-R03 완료 기록

- 차례 시작의 마몬 유지·재굴림 보류 상호작용을 제거하고, 활성 계약 물리 ID를 받는 정상 행동 API로 교체했다.
- 재굴림은 주사위를 갱신한 뒤 일반 행동을 소모한다. 6은 즉시 계약 효과 버스트를 해결하고, 생존한 모든 경로는 상대 차례로 넘어간다.
- 레비아탄은 같은 리볼버 물리 카드 컨텍스트에서 기존 리볼버 처리기를 최대 두 번 시작하며, 각 발동에 새 선언 상호작용을 만든다.
- 첫 발동이 전투를 끝내면 연속 처리를 중단한다. 두 번째 뒤 상대가 생존하면 버스트 방지나 선언 성공 여부와 관계없이 소유자 영혼 1을 차감한다.
- 적 마몬은 공개된 현재 주사위와 활성 계약 출처만 보고 1~2에서 재굴림한다. 적 레비아탄도 플레이어와 같은 연속 처리와 정보 은닉을 사용한다.
- 안전 결과에는 발동별 성공 여부·버스트 대상·지불 영혼만 포함하고 비공개 카드 숫자와 덱 순서는 포함하지 않는다.
- 여러 레비아탄 계약의 추가 발동 중첩은 기획 미확정으로 남겼으며, 현재 기본 계약 제한에서는 첫 유효 활성 계약 하나만 적용한다.
- Unity MCP에서 전용 14/14(job `a7bd42c80d65412cac84a55472d06a3f`), CoreLoop 367/367(job `8d0888f4f18140a6b4ac3f8b3408ddc1`), 전체 EditMode 556/556(job `25822e798ef4400d8b241de3cdac1f43`)을 통과했다.
- `GameScene`·씬·프리팹·Packages·HONG RunFlow/Shop·Shim0Hwan 아트·외부 에셋·오픈소스는 변경하지 않았다. 월드 활성 계약 입력은 DC-R05 범위다.

### CU-M07 현행 레비아탄 정정 완료 기록

- 첫 성공은 상대 버스트가 방지되어도 두 번째 발동 없이 종료하고 영혼을 지불하지 않는다.
- 첫 실패만 `ResolvedWithRetry` 결과 연출 뒤 새 선언을 요청한다. 두 번째 성공은 영혼 비용 0, 두 번째 실패는 영혼 비용 1이다.
- `GameManager`는 첫 실패 연출 뒤 기존 리볼버를 준비 상태로 되돌리고 최종 판정에는 일반 성공·실패 연출을 사용한다.
- 대상 4/4, 관련 마몬·레비아탄 18/18(job `35bd1df8d35e467d9fdbaaf1359027f2`), CoreLoop 442/442(job `0c51483524f74466995c4f33b6b15f3d`), 전체 631/631(job `4546b3326ead476590aa8bfa422cd3e2`)을 통과했다.
- GameScene에서 `Ready → ResolvedWithRetry → Ready → Resolved`, 최종 실패 Animator 상태와 영혼 11→10을 확인했다. 씬·프리팹·Animator Controller·Packages·외부 에셋은 변경하지 않았다.

### DC-R04 구현 순서

1. 바알제붑과 메피스토펠레스: 기존 버스트·수동 카드 결과 훅 확장 — 완료, 바알제붑 직접 선택 정정 포함
2. 아스모데우스와 아자젤: 차례 시작·카드 사용 제한·히트 반복 훅 확장 — 완료
3. 파이몬과 벨리알: 카드 확인·추방·현재 소유권과 전투 종료 복구 — 완료
4. 바포메트: 오망성 생성·덱 소진·초기화·재삽입 — 완료
5. 루시퍼: 후보 5장과 중첩 계약 조정 — 완료

### DC-R04 2/5 완료 기록

- 바알제붑의 자동 최근 카드 폐기를 제거하고 계약 소유자가 자신의 공개 카드와 상대 공개 카드 1장씩을 고르는 두 단계 상호작용으로 교체했다. 영혼 0과 오래된 선택은 카드 이동 없이 종료·거절한다.
- 아스모데우스는 소유자 정상 차례 시작에 선택형 상대 공개 히트를 제공하고, 사용 안 함과 강제 히트 모두 정상 행동을 보존한다. 숫자 6 이하 수동 카드는 별도 불가 사유로 차단한다.
- 아자젤은 계약 즉시와 정상 히트 뒤 사용 완료 공개 수동 카드를 재활성화한다. 동일 숫자 공개 카드 유입은 자동 카드보다 먼저 계약 버스트로 해결하고 기존 사탄·바알제붑 처리를 재사용한다.
- 플레이어·적 대칭 전용 11/11(job `b8b92acf045f4b71a6381b2e57017f22`), 400시드 분포(job `218b0e9f70634351a5b6ea770d810f33`), 100회 자동 전투(job `494c97c286fe4722a6dd96c3a7800fe3`), CoreLoop 390/390(job `48687d06c16f48308cc4351ecfb34177`)을 통과했다. 바알제붑 정정 전용 8/8(job `a95795e446274ab48a97941cadd26634`)과 전체 EditMode 579/579(job `ef2c41ae6a3a4ecfac58cd9d076c7f87`)도 최종 확인했다.
- 최종 Console 6건은 Test Framework 준비·결과 저장·정리 안내이며 컴파일·게임 코드 오류는 0이다. `GameScene`·씬·프리팹·Packages·HONG RunFlow/Shop·Shim0Hwan 아트·외부 에셋·오픈소스는 변경하지 않았다.

### DC-R04 3/5 완료 기록

- 파이몬은 상대 버스트 뒤 양쪽 중 가능한 덱 하나의 위 2장을 비공개로 분리하고, 건너뛰기 또는 한 장 추방 뒤 남은 카드 순서를 복구한다. 추방 카드는 전투 종료 시 원소유자 덱으로 돌아간다.
- 파이몬 대가는 카드 선택 뒤 기존 라운드 결과의 임시 승자에게 한 번만 적용한다. 전체 합 18 이하이면 기존 버스트 방지·대체 경계를 거쳐 승패를 뒤집고 같은 라운드의 재연쇄는 막는다.
- 벨리알은 계약 즉시 소유자의 공개 카드를 모두 버리고, 매 정상 차례 시작에 상대 공개 카드 탈취를 선택하게 한다. 전투용 대리 카드가 자동·수동 효과를 즉시 사용하고 정상 행동을 보존하며 상대 스탠드를 취소한다.
- 벨리알 원본·대리 카드 연결은 양측 카드 ID 충돌을 피하고 라운드 종료 뒤 현 소유자 버린 더미, 전투 종료 뒤 대리 제거·원소유자 복구를 보장한다. 새 라운드 드로우 전 영혼 1로 0이 되면 즉시 전투가 끝난다.
- 파이몬·벨리알 전용 10/10(job `b5e5706ba044447ba4bd63c84f7ee364`), 광신도 정책·400시드·100자동전투 묶음 9/9(job `fd969132ba944260af874e4f51e8c82e`), CoreLoop 402/402(job `aa6c935ffe134a3ea8332b8256156849`), 전체 EditMode 591/591(job `56d59206ba42450a958d777be261f141`)을 통과했다.
- `CoreLoop` UnityEngine 참조, `GameScene`·씬·프리팹·Packages·HONG RunFlow/Shop·Shim0Hwan 아트·외부 에셋·오픈소스 변경은 없다. 월드 계약 입력·두 해상도는 DC-R05 범위다.

### DC-R04 4/5 완료 기록

- 바포메트 활성화는 계약 인스턴스마다 소유자 덱에 1~3 오망성 3장, 상대 덱에 1~5 오망성 5장을 전투 한정 물리 ID로 섞는다. 숫자 1은 기존 에이스 합산을 따른다.
- 특정 묶음이 대상 드로우 더미에서 모두 빠진 안전 경계에 손·드로우·버린 더미·파이몬 추방·벨리알 탈취 대리의 해당 오망성을 회수한다. 가용 드로우·버린 더미를 다시 섞고 같은 묶음을 새 ID로 삽입한 뒤 해당 대상을 계약 효과 버스트시킨다.
- 버스트는 기존 사탄 절대 방지와 바알제붑 대체를 재사용하고, 방지되면 다음 소진 묶음 또는 원래 행동을 이어간다. 숫자·카드·다른 계약 버스트가 이미 확정된 동일 진입에서는 기존 판정을 먼저 유지한다.
- 적 광신도는 공개 후보만으로 바포메트를 일반 선호 계약으로 선택한다. 실제 드로우 더미 소진과 물리 ID는 정책 관측에 포함하지 않는다.
- 바포메트 전용 7/7(job `2ca20df90b4843be9e0ea0fd8c0d9802`), 광신도 정책·400시드·100자동전투 9/9(job `08d0462cbc8c47d3a795dc3fae2ea03d`), CoreLoop 410/410(job `1587c933625e4c88a4a7a838a2529b1b`), 전체 EditMode 599/599(job `a3f94c75cae14666b2c0121991036c07`)를 통과했다.
- `GameScene`·씬·프리팹·Packages·HONG RunFlow/Shop·Shim0Hwan 아트·외부 에셋·오픈소스는 변경하지 않았다. 소유자 숫자와 드로우 더미 소진 정의는 플레이 테스트 후 재검토한다.

### DC-R04 5/5 완료 기록

- 현재 계약 소유자의 남은 악마 덱에서 최대 5장을 꺼내 실제 카드와 `추가 계약하지 않음`을 함께 제시한다. 후보가 없으면 루시퍼 개별 영혼 1만 적용하고 선택 없이 행동을 완료한다.
- 선택하지 않은 후보는 악마 버린 더미로 보내고 선택 카드는 새 활성 계약 인스턴스로 만든다. 최초 기본 계약 비용과 전투당 사용 횟수는 다시 부과하지 않으며, 선택 악마의 능력과 개별 대가만 기존 Resolver로 실행한다.
- 같은 악마와 루시퍼 재선택을 물리 카드 ID·런타임 상태별로 분리했다. 루시퍼 연쇄는 줄어드는 실제 악마 덱을 사용해 유한하며, 복합 대가 영혼 0과 선택 마몬 6은 기존 즉시 사망·계약 버스트 경계를 재사용한다.
- 광신도는 공개된 계약 후보 종류와 정의 키만 받아 생존 가능한 루시퍼와 유효한 후속 계약을 고른다. 후보가 해롭거나 무용하면 건너뛰기를 선택할 수 있고 숨은 카드 값·덱 순서는 관측하지 않는다.
- 루시퍼 전용 10/10(job `042c963a9ff142249e8a96f849d5f5f0`), CoreLoop 421/421(job `ef1377618e1141a2909df95a04c0adfe`), 전체 EditMode 610/610(job `a8c73ceb5b5647d5830cada513705721`)을 통과했다. 컴파일 Console 오류는 0이었다.
- `GameScene`·씬·프리팹·Packages·HONG RunFlow/Shop·Shim0Hwan 아트·외부 에셋·오픈소스는 변경하지 않았다. 실제 월드 입력·시작 악마 선택 연결·두 해상도는 DC-R05 범위다.

### DC-R05 완료 기록

- 실제 `StageTest`에서 시작 악마 2장 중 사탄 1장을 선택하고 `OpponentSelection`으로 전이한 뒤, Play Mode 재진입과 `CONTINUE RUN`을 거쳐 `satan` 1장 상태가 복원되는 것을 확인했다.
- 활성 마몬·사탄의 물리 계약 카드 ID를 전투 도메인이 재검증하는 공통 `Try*` 입력으로 연결하고 두 세션·`CoreLoopView/Controller`·`GameManager`가 같은 안전 표시 모델을 사용하게 했다.
- 일반 계약과 루시퍼 추가 계약은 같은 후보 카드 레이아웃을 사용한다. CoreLoopTest의 최대 후보 화면은 전투 정보를 축약해 720p·1080p 모두 5장+건너뛰기와 6개 선택 버튼을 보장한다.
- 최종 Unity MCP 재검증은 신규 4/4(job `a6be5c775ca24aadabbbfa41442030ad`), CoreLoop 425/425(job `8aacbbd269444082b8034f6d7ae493bd`), StageProgression 189/189(job `80637a2e41494fc18d79696154f7beb4`)이다.
- `StageTest`, `CoreLoopTest`, `GameScene`을 1280×720·1920×1080으로 확인했다. 시각 판정은 58점→76점 수정 뒤 92점 통과했고 Play Mode를 종료했다.
- Console의 3건은 Test Framework 결과 저장 안내, 6건은 사전·사후 처리 경고이며 컴파일·게임 코드 오류는 0이다. 씬·프리팹·외부 에셋은 변경하지 않았다.
- 앞면 월드 계약 카드·호버 상세·사탄 면 전환은 Shim0Hwan의 아트·씬 소유 경계를 침범하지 않는 별도 시각 작업으로 유지한다.

### DC-R06 — Notion v0.7 적 계약 이관

#### 작업

- 아스모데우스의 사용 제한 상수를 7 이하로 바꾸고 플레이어·적·표시·테스트를 같은 기준으로 이관한다.
- 당시 광신도의 전투 악마 후보를 벨페고르·바알제붑·벨리알로 제한하고 영혼·사용 가능한 공개 카드 수로 결정했다. 이 규칙은 EPR07에서 현행 2종·공개 카드 수 기준으로 교체했다.
- 광신도가 첫 정상 차례부터 계약 성공까지 매 정상 차례 계약을 우선하도록 결정적 정책으로 구현한다.
- 집행자에게 파이몬을 부여하고, 보스에게 바포메트→아스모데우스→아자젤의 고정 계약 단계를 부여한다.
- 보스는 전투 시작에 1단계, 피해 뒤 영혼 5 이하에 2단계, 2 이하에 3단계를 적용하고 복수 경계 통과를 순차 처리하며 회복으로 역행하지 않게 한다.
- 모든 정책은 공개 관측과 자신의 계약 상태만 사용하고 숨은 카드·덱 순서를 읽지 않는다.

#### 완료 게이트

- 적별 계약 후보가 Notion v0.7 목록과 일치한다.
- 7 이하 카드 제한이 플레이어·적 양쪽에서 동일하며 8 이상은 허용된다.
- 광신도 조건 분기, 집행자 파이몬, 보스 단계 순서가 결정적 테스트로 고정된다.
- 기존 CU-M07 레비아탄 조건부 재예측과 플레이어 계약 회귀가 유지된다.
- CoreLoop·StageProgression 전체 EditMode와 관련 화면 검증을 통과하고 실제 결과를 진행 기록에 남긴다.

#### 실제 완료 결과

- EP-R04의 아스모데우스 7 이하 제한과 EP-R05의 광신도·집행자 계약 결과를 유지했다.
- EP-R06에서 보스 고정 단계 불변 데이터와 여섯 악마 덱을 추가하고 프로필 전투 생성 경계에 연결했다.
- 시작·영혼 5·2 전환, 복수 경계 순서, 회복 비역행, 치명 피해 중단과 일반 계약 미소비를 구현했다.
- 기존 계약 표시 모델에 현재 보스 계약 효과를 연결했다. GameScene·씬·프리팹은 변경하지 않았다.
- EPR06 10/10과 전체 EditMode 665/665를 Unity 별도 복제본에서 통과했다.
- 골드·저장·정식 런 UI는 HONG의 RF 범위로 분리하고 DC-R06 완료 조건에서 제외했다.

### DC-V01 — GameScene 일반 계약 월드 후보

#### 구조 변경 반영

- 현재 `GameScene`은 `GameManager`가 전투 세션, Pointer raycast, HUD `OnGUI` 명령과 월드 카드 입력을 함께 조정한다.
- 후보 카드 프리팹은 새 씬 오브젝트를 직접 직렬화하지 않고 `GameManager.prefab`의 `DemonContractSelectionView`가 기존 `DemonCard.prefab`을 참조한다.
- 카드 이름·능력·대가는 기존 순수 표시 모델에서 받고, 앞면 스프라이트는 `GameHudView`의 `CardContentCatalogSO` 참조를 재사용한다.
- 다른 작업이 추가한 해머 월드 카드 대상 선택은 같은 raycast 경로를 공유하므로 그대로 보존하고, 계약 후보가 열린 동안에만 더 높은 우선순위의 모달 분기로 차단한다.

#### 작업

1. `CONTRACT` 명령의 확인·취소 로컬 상태를 제거하고 `TryBeginPlayerDemonContract`로 직접 전달한다.
2. 일반 `ChooseContract` 후보 1~2장만 구분하는 HUD 모드를 추가하고 루시퍼·기타 다중 상호작용은 기존 옵션 UI에 남긴다.
3. 화면 하단 뷰포트 기준 앵커, 반노출 크기, 두 장의 작은 부채꼴 회전과 호버 전진·상승·확대·정렬을 담당하는 `DemonContractSelectionView`를 추가한다.
4. 기존 후보 HUD 패널을 제목·카드 앞면·`ACTIVE`·`COST` 상세로 재사용하고 정의 키에 맞는 SO 스프라이트를 연결한다.
5. 후보 모달 중 월드 후보 호버·클릭과 카메라 `W`·`S`, 일시정지 외 전투·덱·상점 입력을 차단한다.
6. 선택 클릭은 표시 모델의 `InteractionId`·`OptionId`를 기존 세션 `Try*` 경계로 한 번 전달하고, 성공 후 월드 후보와 상세를 즉시 닫는다.
7. Presenter·HUD 프리팹 계약 테스트, 전체 EditMode, 실제 GameScene 후보/호버/선택과 Console을 검증한다.

#### 완료 게이트

- CONTRACT 입력 한 번으로 영혼 비용이 적용되고 일반 후보 1~2장이 열린다. 시작 불가는 무변경이다.
- 후보는 화면 하단에 절반만 보이고 두 장일 때 자연스러운 작은 호를 이룬다.
- 호버 카드는 앞으로 나오고 위로 들리며 확대되고, 큰 패널의 제목·앞면·`ACTIVE`·`COST`가 SO 데이터와 일치한다.
- 후보 선택 중 다른 전투·덱·상점 입력이 실행되지 않으며 `W`·`S`, 호버, 클릭과 일시정지만 유지된다.
- 후보를 클릭하면 정확히 해당 옵션 하나만 선택되고 후보 뷰와 상세가 닫힌다.
- 루시퍼 최대 5장·건너뛰기와 기존 해머 월드 대상 선택 회귀가 유지된다.
- 신규 대상 테스트, 전체 EditMode와 실제 GameScene 확인 결과를 진행 기록에 남긴다.

각 단계의 권장 커밋 제목은 실제 구현 시 Lore 형식으로 작성한다. 문서 단계의 권장 제목은 `개정된 악마 계약을 구현 가능한 순서와 검증 경계로 고정하다`이다.

## 14. 의존성

```text
DC-00 문서 기준
  └─ DC-01 데이터·런/전투 덱
       └─ DC-02 비용·후보·선택
            └─ DC-03 벨페고르 수직 기능
                 └─ DC-04 마몬·레비아탄
                      └─ DC-05 화면·런 연결
                           └─ DC-06 사탄 결정·구현
                                └─ DC-07 적 AI·최종 검증
                                     └─ DC-08 선택 밸런스·실전 검증
                                          └─ DC-R00 최신 기획 이관
                                               └─ DC-R01 시작 선택·후보
                                                    └─ DC-R02 사탄
                                                         └─ DC-R03 마몬·레비아탄
                                                              └─ DC-R04 나머지 악마
                                                                   └─ DC-R05 통합 검증
                                                                        └─ CU-M07 레비아탄 조건부 재예측 정정
```

DC-01~DC-08은 과거 규칙의 구현 이력이다. 현행 완료 판정은 DC-R 단계를 별도로 사용하며 DC-R00~DC-R05의 규칙·입력·저장 재진입·스크립트 UI 검증은 모두 완료됐다. 물리 월드 계약 카드 연출은 계약 기능 완료 판정과 분리한 씬·아트 후속 작업이다.

## 15. 단계별 검증 수준

| 단계 | 단위 테스트 | 통합 테스트 | Game View | 씬·Console |
| --- | --- | --- | --- | --- |
| DC-00 | 미실행 | 미실행 | 미실행 | 미실행 |
| DC-01 | 필수 | 전투 변환 | 해당 없음 | Console 컴파일 확인 |
| DC-02 | 필수 | 세션 전달 | 해당 없음 | Console 컴파일 확인 |
| DC-03 | 필수 | 독립·런 | 선택 모델 확인 | Console 확인 |
| DC-04 | 필수 | 카드·라운드 | 결과 모델 확인 | Console 확인 |
| DC-05 | 필수 | 독립·런·보상 | 두 해상도 | 두 씬·Console |
| DC-06 | 필수 | 권능·종료 | 카운터·효과 | 두 씬·Console |
| DC-07 | 12/12 | 389/389 | 기존 반응형 배치 유지·Full HD 스모크 | 최종 Error/Warning 0 |
| DC-08 | 8/8 | 397/397 | 기존 표시 Full HD 스모크 | 최종 Error/Warning 0 |
| DC-R01 | 14/14 | 511/511 | 미실행, DC-R05 연결 범위 | 컴파일 오류 0·씬 에셋 무변경 |
| DC-R02 | 12/12 | CoreLoop 363/363·전체 553/553 | 표시 모델 테스트, 월드 카드 입력은 DC-R05 | 컴파일 게임 오류 0·씬 에셋 무변경 |
| DC-M01 | CoreLoop 362/362 | 전체 551/551 | 활성 계약 후 상시 패널 미표시 | GameScene·Console 0, 씬 에셋 무변경 |
| DC-R04 3/5 | 파이몬·벨리알 10/10·광신도 9/9 | CoreLoop 402/402·전체 591/591 | 미실행, DC-R05 연결 범위 | 컴파일·게임 코드 오류 0·씬 에셋 무변경 |
| DC-R04 4/5 | 바포메트 7/7·광신도 9/9 | CoreLoop 410/410·전체 599/599 | 미실행, DC-R05 연결 범위 | 컴파일·게임 코드 오류 0·씬 에셋 무변경 |
| DC-R04 5/5 | 루시퍼 10/10 | CoreLoop 421/421·전체 610/610 | 미실행, DC-R05 연결 범위 | 컴파일·게임 코드 오류 0·씬 에셋 무변경 |
| DC-R05 | 신규 4/4 | CoreLoop 425/425·StageProgression 189/189·합계 614/614 | 저장 재진입·`StageTest`·`CoreLoopTest`·`GameScene` 1280×720/1920×1080·시각 92점 | 컴파일·게임 코드 오류 0, Play Mode 종료·씬/프리팹 무변경 |
| CU-M07 | 신규 4/4 | CoreLoop 442/442·전체 631/631 | GameScene 첫 실패·재준비·최종 실패 상태 | 컴파일·게임 코드 오류 0, 씬·프리팹·Controller 무변경 |
| DC-V01 | GameScene HUD/프리팹 10/10 | 787개 중 광신도 자동전투 1건 별도 실패 | 하단 반노출 1~2장·호버 포즈·상세·클릭 720p/1080p 확인 | 컴파일 오류 0·Play Mode 종료, 기존 씬 변경 보존 |

## 16. 위험과 대응

| 위험 | 영향 | 대응 |
| --- | --- | --- |
| 계약을 카드 효과에 합침 | 상태·덱·수명 혼동 | 별도 정의·덱·Resolver 유지, 결과 훅만 연결 |
| 조회 중 후보·난수 소비 | UI 조회로 결과 변화 | 무변경 가용성 조회와 실행 시점 생성 분리 |
| 비용만 지불하고 후보 실패 | 회복 불가능한 상태 | 가용 카드 1장 이상을 사전 검증한 뒤 실제 1~2장 후보와 비용을 전이 |
| 선택 중 다른 입력 실행 | 중복 비용·차례 꼬임 | 단일 계약 처리 상태와 InteractionId 재검증 |
| 숨은 합 노출 | AI·플레이어 공정성 붕괴 | 결과에 성공 여부만 저장, 정보 은닉 테스트 |
| 임시 카드가 런 덱에 잔존 | 다음 전투 오염·소유권 파괴 | 물리 출처 추적과 전투 종료 원상복구 테스트 |
| 같은 악마 상태가 합쳐짐 | 카운터·주사위·대가 누락 | 계약 인스턴스 ID별 상태와 비용 처리 |
| 마몬이 동기식 라운드 해결을 막음 | 상태 교착 | 재굴림은 정상 행동으로 즉시 종료하고 최종 적용만 보류 상호작용 유지 |
| 사탄 규칙 충돌 | 문서·코드 불일치 | DC-D05·D06 확정값과 전용 회귀 테스트로 잠금 |
| 정식 런 담당과 충돌 | 병합 비용 증가 | HONG의 RunFlow·Shop 영역을 변경하지 않음 |
| 고정 계약 점수로 선택 편향 | 일부 악마만 반복 선택 | 효용 조건과 시드 결정 후보 순서 동점 분산, 400시드 회귀 |
| 루시퍼 연쇄가 비용·횟수를 중복 부과하거나 무한 반복 | 영혼·계약 상태 오염·교착 | 실제 물리 덱을 소모하고 추가 계약은 개별 대가만 적용, 동일 악마·재귀·고갈 회귀 |

## 17. 커밋 원칙

- 단계 하나의 코드·테스트·문서를 하나의 검토 가능한 커밋으로 묶는다.
- 첫 줄은 Lore 규칙에 따라 변경 내용보다 변경 의도를 설명한다.
- 본문에는 제약, 선택 이유, 거절한 대안과 검증·미검증 항목을 기록한다.
- DC-00 문서 커밋과 DC-01 구현 커밋을 분리한다.
- 실패한 실험이나 임시 규칙을 완료 기록으로 숨기지 않는다.

## 18. 변경 기록

| 날짜 | 작성자 | 변경 |
| --- | --- | --- |
| 2026-07-30 | HONG | 현재 GameManager·HUD·월드 카드 구조에 맞춰 DC-V01을 추가했다. 즉시 계약, 일반 1~2장 전용 월드 후보, SO 상세, 모달 입력과 루시퍼/해머 회귀를 구현·검증 단계로 분리했다. |
| 2026-07-29 | 이천서 | EP-R06 보스 고정 계약의 시작·5·2 전이와 생존·복수 경계·비역행·일반 계약 차단·표시를 구현하고 전용 10/10·전체 665/665로 검증해 DC-R06 적별 계약 이관 완료 |
| 2026-07-29 | 이천서 | EP-R05에서 DC-R06 광신도 전용 계약·재시도와 집행자 파이몬을 구현해 정책 12/12·CoreLoop 466/466·전체 655/655로 검증하고 보스 단계만 다음 작업으로 유지 |
| 2026-07-29 | 이천서 | EP-R04에서 DC-R06 중 아스모데우스 7 이하 제한을 선행 완료하고, 광신도·집행자·보스의 계약 구성만 다음 계약 작업으로 유지 |
| 2026-07-29 | 이천서 | DC-R06의 광신도 계약 재시도와 보스 전투 시작·영혼 5·2 계약 전환을 확정해 기획 결정 선행 조건을 해소 |
| 2026-07-29 | 이천서 | CU-M07 레비아탄 첫 실패 조건부 재예측·두 번 실패 영혼 1·GameScene 연출을 구현하고 대상 4/4·CoreLoop 442/442·전체 631/631로 검증해 과거 DC-R03 규칙을 현행 정정 |
| 2026-07-28 | 이천서 | DC-R05 시작 선택·저장 재진입·활성 계약 입력·루시퍼 후보 레이아웃을 신규 4/4·CoreLoop 425/425·StageProgression 189/189·두 전투 화면 720p/1080p·시각 92점으로 완료하고 물리 월드 카드 연출은 별도 씬·아트 작업으로 분리 |
| 2026-07-28 | 이천서 | DC-R04 5/5 루시퍼의 현재 악마 덱 최대 5장·건너뛰기·별도 계약 인스턴스·복합 대가·재귀 연쇄·광신도 선택을 구현해 전용 10/10·CoreLoop 421/421·전체 610/610으로 검증, 다음 단계를 DC-R05 통합으로 전환 |
| 2026-07-28 | 이천서 | DC-R04 4/5 바포메트 오망성 양측 묶음·드로우 더미 소진·전투 위치 회수·가용 덱 초기화·재삽입·기존 버스트 경계를 구현해 전용 7/7·광신도 9/9·CoreLoop 410/410·전체 599/599 검증, 다음 묶음을 루시퍼로 전환 |
| 2026-07-28 | 이천서 | DC-R04 3/5 파이몬 선택 추방·단일 임시 승자 대가와 벨리알 공개 카드 탈취·즉시 재사용·스탠드 취소·라운드 대가·전투 종료 소유권 복구를 구현해 전용 10/10·광신도 9/9·CoreLoop 402/402·전체 591/591 검증, 다음 묶음을 바포메트로 전환 |
| 2026-07-28 | 이천서 | 바알제붑 폐기 대상을 양측 직접 선택으로 정정하고 DC-R04 2/5 아스모데우스·아자젤의 차례 시작 선택·카드 제한·재활성화·동일 숫자 선행 버스트를 구현해 바알제붑 8/8·신규 11/11·CoreLoop 390/390·전체 579/579 검증, 다음 묶음을 파이몬·벨리알로 전환 |
| 2026-07-28 | 이천서 | DC-R04 1/5 바알제붑·메피스토펠레스의 버스트 대체·최종 승부 재계산·스탠드 제한·보위 나이프 비버스트 대가·적 선택을 구현해 전용 8/8·CoreLoop 377/377·전체 566/566 검증, 다음 묶음을 아스모데우스·아자젤로 전환 |
| 2026-07-28 | 이천서 | DC-R03 마몬 재굴림의 행동 소비·차례 종료와 레비아탄의 독립 선언 2회·첫 종료 중단·최종 생존 대가를 구현해 전용 14/14·CoreLoop 367/367·전체 556/556으로 검증, 다음 단계를 DC-R04로 전환 |
| 2026-07-28 | 이천서 | DC-R02 사탄 카운트 4·소유자 차례 시작/종료·영혼 대가 1회·활성 계약 양면 행동·자동 카드 재개·적 대칭을 구현하고 별도 권능 카드를 제거해 전용 12/12·CoreLoop 363/363·전체 553/553으로 검증, 다음 단계를 DC-R03으로 전환 |
| 2026-07-28 | 이천서 | DC-M01에서 계약 확인·후보 선택은 유지하고 완료 후 상시 `DEMON CONTRACT` 상태 패널만 제거해 CoreLoop 362/362·전체 551/551·GameScene·Console 0으로 검증 |
| 2026-07-27 | 이천서 | DC-R01 시작 악마 제안·선택, 일반 후보 0·1·2장, 루시퍼 5장 경계와 시작 체크포인트를 구현하고 전용 14/14·전체 EditMode 511/511로 검증한 뒤 다음 단계를 DC-R02로 전환 |
| 2026-07-27 | 이천서 | 최신 노션의 가변 후보·시작 선택·사탄·마몬·레비아탄·메피스토펠레스와 나머지 악마 개정을 DC-R00~DC-R05로 분리하고 문서 이관만 완료로 기록 |
| 2026-07-22 | 이천서 | DC-00~DC-07 순차 구현, 사탄 결정 게이트, 단계별 검증과 한글 Lore 의도 제목 수립 |
| 2026-07-22 | 이천서 | 개별 대가 즉시 사망, 동일 악마 인스턴스, 계약 임시 카드의 라운드 유지·전투 종료 복구를 단계별 작업과 검증에 반영 |
| 2026-07-22 | 이천서 | DC-01 정의·런/전투 덱·후보 보충·전투 변환과 19개 대상·329개 전체 회귀 완료, 다음 단계를 DC-02로 전환 |
| 2026-07-22 | 이천서 | DC-02 엄격한 영혼 비용·횟수·후보 선택·상호작용 ID·활성 계약·세션 전달과 13개 대상·342개 전체 회귀 완료, 다음 단계를 DC-03으로 전환 |
| 2026-07-22 | 이천서 | DC-03 벨페고르 미리보기·동일 ID 히트/덱 아래·자동 스탠드·정보 은닉과 9개 대상·351개 전체 회귀 완료, 다음 단계를 DC-04로 전환 |
| 2026-07-22 | 이천서 | DC-04 마몬 주사위·최종 합 선택과 레비아탄 리볼버 후속 판정·영혼 대가를 구현하고 대상 11/11·전체 362/362 뒤 다음 단계를 DC-05로 전환 |
| 2026-07-23 | 이천서 | DC-05 계약 표시 모델·지불 전 확인·후보/후속 입력·활성 결과·독립/런 UI를 구현하고 대상 7/7·전체 369/369·두 씬·두 해상도 뒤 다음 단계를 DC-06 결정으로 전환 |
| 2026-07-23 | 이천서 | DC-06 정상 차례 카운터·스탠드/버스트 제한·영혼 대가·양면 권능·전투 종료 정리를 구현하고 대상 8/8·전체 377/377·두 씬·두 해상도·Console 0 뒤 다음 단계를 DC-07로 전환 |
| 2026-07-23 | 이천서 | DC-07 광신도 계약 정책·적 소유 4종 대칭 처리·Cultist 전용 전투 덱·안전 표시와 반복 회귀를 구현하고 대상 12/12·CoreLoop 260/260·전체 389/389·Console 0으로 우선 악마 4종 계획을 완료 |
| 2026-07-23 | 이천서 | DC-08 사탄 생존·레비아탄 리볼버 효용 조건과 벨페고르·마몬 결정적 균형 분산을 구현하고 400시드·100자동전투·대상 8/8·CoreLoop 268/268·전체 397/397 검증 기준을 완료 |
