# 카드 사용 시스템 기획서

> 프로젝트: DiaBlackJack  
> 기획·문서 책임자: 이천서  
> 버전: v0.17
> 상태: 독극물 문구 개정·프로토타입 악마 7종·CU-M09 해머 직접 선택 반영
> 최종 갱신: 2026-07-30

> **현행 악마 범위:** 사탄·벨페고르·바알제붑·아스모데우스·마몬·바포메트·아자젤만 프로토타입에 등장한다. 루시퍼·레비아탄·메피스토펠레스·파이몬·벨리알 항목은 비활성 보관 기획이며 플레이어·적·상점·후보 풀에서 제외한다.

## 1. 문서 목적

이 문서는 `rule.md`의 카드 사용 규칙을 현재 전투 구조에서 바로 구현할 수 있는 범위로 구체화한다. 폴드는 현행 행동에서 삭제되었으며, 폴드에 의존하던 독극물과 루시퍼는 각각 즉시 스탠드 선택과 영혼 1 대가로 개정한다.

이번 작업은 플레이어가 일반 카드의 효과를 직접 사용하는 경험을 완성하는 데 집중한다. 자동 발동 카드, 악마 계약, 특수 카드와 적의 자율 카드 사용은 같은 효과 처리 기반을 재사용할 수 있지만 이번 완료 조건에는 포함하지 않는다.

규칙 원문에 없는 처리 순서나 모호한 카드 문구는 프로토타입 임시 결정으로 명시한다. 최종 기획 승인과 변경 결정은 이천서가 담당한다.

## 2. 기준 문서와 우선순위

| 우선순위 | 문서 | 적용 방식 |
| ---: | --- | --- |
| 1 | `Docs/card-use-design.md` | 카드 사용 작업의 확정 범위와 프로토타입 결정 |
| 2 | `Docs/card-use-development-spec.md` | 코드 구조, 공개 API, 상태와 테스트 기준 |
| 3 | `Docs/game-design-document.md` | 전체 게임 기획과 공통 카드 규칙 |
| 4 | `Docs/rule.md` | 원본 규칙과 카드별 의도 |

상위 문서가 하위 문서의 원문을 영구 변경하는 것은 아니다. 구현 과정에서 충돌이 발견되면 `card-use-progress-log.md`에 결정과 이유를 기록한다.

## 3. 기획 결정 요약

| 항목 | 프로토타입 결정 |
| --- | --- |
| 작업 식별자 | `CU`(Card Use) |
| 1차 사용자 | 플레이어만 직접 카드를 사용한다. |
| 포함 카드 | 수정 구슬(5), 위협용 해머(6), 리볼버(7·8), 보위 나이프(9·10) |
| 수동 사용 불가 | 에이스(패시브), 효과 없는 2·3·4 |
| 사용 횟수 | 한 물리 카드 인스턴스는 손에 있는 동안 한 번만 사용한다. |
| 차례 소비 | 효과가 정상적으로 끝나면 차례 하나를 소비한다. |
| 비공개 카드 | 사용 승인과 동시에 앞면 공개하되 최종 승부 전까지 비공개 역할을 유지한다. 숫자는 공개 정보가 된다. |
| 사용 후 위치 | 별도 문구가 없으면 손에 남고 합계에도 포함된다. |
| 행동 취소 | 사용이 승인된 뒤에는 취소할 수 없다. 승인 전 UI 탐색은 차례를 소비하지 않는다. |
| 효과 종료 | 효과 명령 뒤마다 버스트와 전투 종료를 확인한다. 종료되면 남은 효과와 적 차례를 실행하지 않는다. |
| 자동 발동·계약 | 이번 작업에서 제외한다. 효과 실행 기반만 재사용 가능하게 둔다. |

## 4. 작업 범위

### 4.1 포함 범위

- 카드 정의와 전투 중 카드 인스턴스의 사용 가능·처리 중·사용 완료 상태
- 공개 카드 사용 및 비공개 카드 공개 후 사용
- 카드별 사용 조건과 유효 대상 검사
- 카드 효과를 위한 숫자·카드 선택
- 효과 처리 중 카드 공개, 드로우, 버림, 비공개 카드 교체, 강제 히트와 직접 버스트
- 카드 효과로 발생한 즉시 라운드 종료와 영혼 피해
- 효과 종료 후 적 차례 또는 다음 플레이어 차례 연결
- 최소 전투 화면의 카드 사용·선택·결과 표시
- 런 전투에서 카드 사용 결과와 지속 영혼 동기화
- 규칙, 표시, 세션, 진행 연결의 자동·수동 검증

### 4.2 제외 범위

- 자동 발동 카드 5종
- 악마 계약과 악마 패시브에 의한 카드 효과 변경
- 바포메트의 오망성 등 계약 생성 특수 카드
- 적이 스스로 카드 사용 행동을 고르는 AI
- 카드 보상, 강화, 삭제와 덱 편집 UI
- 카드 애니메이션, 최종 일러스트, 사운드와 현지화 마감
- 범용 스크립팅 언어, 문자열 기반 효과 해석기와 외부 데이터 포맷
- 저장 파일 버전 변경

제외 항목은 카드 사용 작업의 완료를 막지 않는다. 다만 신규 구조가 해당 기능을 불필요하게 차단해서는 안 된다.

## 5. 공통 카드 사용 규칙

### 5.1 사용 조건

다음 조건을 모두 만족해야 카드를 사용할 수 있다.

1. 전투가 진행 중이고 현재 상태가 플레이어 행동 가능 상태다.
2. 선택한 카드가 플레이어 손에 실제로 존재한다.
3. 카드 정의가 `수동 사용` 유형이다.
4. 해당 카드가 아직 사용되지 않았고 다른 효과를 처리 중이지 않다.
5. 카드별 필수 대상, 덱 잔량과 선택지가 존재한다.
6. 런 전투라면 진행 상태가 `InBattle`이다.

검사 실패는 `false`를 반환하며 카드 공개, 사용 표시, 카드 이동, 차례 변경을 포함한 어떤 상태도 바꾸지 않는다.

### 5.2 사용 처리 순서

1. 공통 조건과 카드별 사전 조건을 모두 검사한다.
2. 비공개 사용 카드라면 공개한다.
3. 사용 카드를 `처리 중`으로 잠근다.
4. 필요한 선택지를 플레이어에게 제시한다.
5. 카드별 효과를 정해진 순서대로 실행한다.
6. 각 효과 명령 뒤 버스트·라운드·전투 종료를 확인한다.
7. 전투가 계속되면 사용 카드를 `사용 완료`로 표시한다.
8. 효과가 완전히 끝났으면 차례를 소비하고 적 차례를 실행한다.
9. 적이 이미 스탠드 상태라면 기존 규칙에 따라 플레이어에게 차례가 돌아올 수 있다.

효과가 라운드나 전투를 끝냈다면 8~9단계를 실행하지 않는다. 사용 카드가 효과 중 버려져도 해당 인스턴스의 사용 완료 상태는 유지되며, 덱에서 다시 뽑힐 때 초기화한다.

### 5.3 공개 정보와 선택 취소

- 비공개 카드는 사용 승인 시 앞면 공개되며 다시 숨기지 않는다. 앞면·뒷면 표시 상태와 공개·비공개 역할은 별도 상태다.
- 앞면 공개된 비공개 역할 카드는 숫자와 효과만 양측 공개 정보가 되며, 공개 합·중간 버스트·공개 카드 대상에서 제외한다.
- 체인지·해머 교체·비공개 추측·최종 승부와 양측 손패 왼쪽 배치에서는 계속 비공개 카드 한 장으로 취급한다.
- 덱 위 카드를 확인하는 효과는 카드 소유자에게만 실제 숫자를 표시한다.
- 아직 앞면 공개되지 않은 상대 비공개 값은 효과 판정 계층에서만 비교하고 UI나 일반 표시 모델에 노출하지 않는다. 이미 앞면 공개된 비공개 숫자는 일반 공개 정보로 표시한다.
- 사용이 승인된 뒤에는 취소할 수 없다. 확인한 정보만 얻고 행동을 되돌리는 악용을 막기 위한 결정이다.
- UI에서 카드 설명을 열거나 사용 가능 여부를 확인하는 행위는 사용 승인이 아니며 자유롭게 취소할 수 있다.

### 5.5 적 리볼버의 확정 대응

플레이어 비공개 카드가 카드 사용·효과·계약 등 어떤 이유로든 앞면 공개되었고 적이 현재 사용할 수 있는 리볼버를 보유하면, 적은 성향별 점수와 기존 확률 임계값보다 리볼버를 우선한다. 선언 숫자는 공개된 비공개 카드 숫자와 같아야 한다. 아직 공개되지 않은 비공개 카드에는 기존 공개 정보 기반 추론만 사용한다.

### 5.4 사용 상태 수명

- `사용 가능`: 손에 들어온 수동 카드의 초기 상태
- `처리 중`: 사용이 승인되어 효과나 선택이 끝나지 않은 상태
- `사용 완료`: 이번에 손에 들어온 뒤 효과를 이미 사용한 상태

카드가 버린 더미와 덱을 거쳐 다시 손에 들어올 때 `사용 가능`으로 초기화한다. 단순히 공개·비공개 상태가 바뀌거나 손 안에서 위치가 바뀌는 것만으로는 초기화하지 않는다.

## 6. 1차 카드별 확정 규칙

### 6.1 수정 구슬 — 숫자 5

#### 사용 조건

- 플레이어 덱에서 카드 2장을 확보할 수 있어야 한다.

#### 처리

1. 덱 위 카드 2장을 덱에서 임시로 분리해 플레이어만 확인한다.
2. `가져오지 않음`, 첫 번째 카드, 두 번째 카드 중 하나를 선택한다.
3. 선택한 카드가 있으면 공개 상태로 플레이어 손에 추가한다.
4. 선택하지 않은 카드는 덱 위로 되돌린다.
5. 되돌아간 카드끼리의 기존 상대 순서는 유지한다.
6. 아무 카드도 고르지 않으면 다음 드로우 순서는 효과 사용 전과 같다.
7. 공개 카드가 추가되었다면 플레이어 버스트 여부를 즉시 확인한다.

수정 구슬 자체는 손에 남고 사용 완료 상태가 된다. 이번 범위에는 자동 발동 카드가 덱에 없으므로 선택 카드의 자동 효과는 처리하지 않는다.

### 6.2 위협용 해머 — 숫자 6

위협용 해머는 **상대의 공개 카드 1장을 제거하는 공격 카드**다. 자신의 공개 카드를 비용으로 버리게 한 이전 세부 기획은 폐기한다.

#### 사용 조건

- 상대에게 선택 가능한 공개 카드가 한 장 이상 있어야 한다.
- 상대가 스탠드 상태라면 상대에게 비공개 카드가 정확히 한 장 있고 상대 덱이 한 장을 제공할 수 있어야 한다.

#### 처리

1. 플레이어가 상대의 공개 카드 한 장을 선택해 버린다.
2. 상대가 스탠드 상태가 아니면 효과를 끝낸다.
3. 상대가 스탠드 상태라면 스탠드를 취소한다.
4. 상대의 기존 비공개 카드를 공개하지 않고 버린다.
5. 상대 덱에서 새 카드 한 장을 비공개 상태로 받게 한다.
6. 교체 결과 상대가 버스트 조건을 만족하면 즉시 라운드를 끝낸다.

해머 자체는 별도 문구가 없으므로 플레이어의 손에 남고 사용 완료 상태가 된다. 상대가 스탠드 상태인데 비공개 카드 교체를 완전하게 수행할 수 없다면 사용 자체를 승인하지 않는다.

### 6.3 리볼버 — 숫자 7·8

#### 사용 조건

- 상대에게 비공개 카드가 정확히 한 장 있어야 한다.

기본 라운드 규칙상 상대 비공개 카드는 항상 한 장이며 두 장 이상인 정상 상태는 없다. 다중 비공개 카드를 위한 별도 선택·추측 규칙은 지원하지 않고, 비공개 카드가 없으면 대상 없음으로 사용을 거절한다.

#### 처리

1. 플레이어에게 1~10 중 숫자 하나를 선택하게 한다.
2. 선택한 숫자를 상대 비공개 카드 숫자와 내부적으로 비교한다.
3. 같으면 상대를 카드 효과 버스트로 처리한다.
4. 다르면 추가 효과 없이 끝낸다.

아직 앞면 공개되지 않은 비공개 카드 숫자는 비교를 위해 새로 공개하지 않는다. 이미 앞면 공개된 비공개 숫자는 기존 공개 정보로 유지한다. 성공 UI는 선택 숫자가 맞았다는 사실만, 실패 UI는 틀렸다는 사실만 표시한다. 직접 버스트는 일반 적 버스트와 동일하게 적 영혼 1 피해를 적용하며 카드 문구에 별도 피해가 있을 때만 추가 피해를 적용한다.

### 6.4 보위 나이프 — 숫자 9·10

#### 사용 조건

- 상대 공개 카드 합이 16 이하여야 한다.
- 상대 덱에서 카드 한 장을 확보할 수 있어야 한다.

#### 처리

1. 상대가 덱에서 카드 한 장을 공개 상태로 강제 드로우한다.
2. 상대의 공개 카드 숫자 합이 21을 초과하면 즉시 숫자 버스트로 라운드를 끝낸다.
3. 공개 합이 21 이하라면 방금 강제로 뽑은 카드만 즉시 상대 버린 더미로 이동한다.
4. 버스트하지 않은 강제 드로우 카드를 유지하거나 선택하는 정책은 두지 않는다.

상대 비공개 카드는 이 중간 판정에 포함하지 않는다. 양쪽 모두 스탠드한 최종 승부에서만 비공개 카드를 포함한 전체 합계를 계산한다.

## 7. 효과 중 승패와 우선순위

효과 처리 중에는 다음 순위를 적용한다.

1. 명시적 카드 이동이나 수치 변경을 한 명령 단위로 완료한다.
2. 직접 버스트 효과를 판정한다.
3. 라운드 진행 중에는 영향을 받은 대상의 공개 카드 합으로 숫자 버스트를 판정한다.
4. 버스트가 있으면 즉시 라운드 피해와 전투 종료를 처리한다.
5. 전투가 계속될 때만 다음 명령이나 선택으로 진행한다.
6. 모든 효과가 끝났을 때만 적 차례로 넘긴다.

같은 명령에서 양측이 동시에 버스트하면 전체 게임 기획서의 결정대로 효과를 발생시킨 쪽이 패배한다. 1차 카드에는 동시 버스트 명령이 없지만 공통 처리 기준에는 포함한다.

## 8. 화면과 입력

### 8.1 평상시

- 플레이어 카드마다 숫자, 이름, 공개 여부, 사용 가능 여부를 표시한다.
- 사용할 수 있는 카드는 `USE` 입력을 제공한다.
- 사용 완료 카드는 `USED`로 표시하고 입력을 비활성화한다.
- 조건이 맞지 않는 카드는 비활성화하되, 최소 디버그 UI에는 짧은 사유를 표시한다.
- 비공개 카드는 플레이어 자신의 카드이므로 숫자와 효과 설명을 볼 수 있다. 상대 화면에는 계속 `?`로 표시한다.

### 8.2 효과 선택 중

- 히트, 스탠드, 체인지, 계약과 다른 카드 사용 입력을 모두 잠근다.
- 한 번에 하나의 질문과 선택지만 표시한다.
- 수정 구슬은 두 카드와 `가져오지 않음`을 표시한다.
- 해머는 버릴 수 있는 상대의 공개 카드만 표시한다.
- 리볼버은 1~10 숫자 선택을 표시한다.
- 처리 중에는 재시작을 제외한 별도 취소 입력을 제공하지 않는다.

### 8.3 결과 피드백

- 사용 카드 이름과 핵심 결과를 한 줄 전투 로그로 남긴다.
- 비공개 정보는 로그에 기록하지 않는다.
- 카드 효과로 버스트하면 일반 히트 버스트와 구분되는 원인을 표시한다.
- 런 승리·패배 화면은 기존 진행 시스템 표시를 그대로 사용한다.

## 9. 기존 시스템과의 경계

| 영역 | 책임 |
| --- | --- |
| 카드 정의 | 카드 이름, 숫자, 발동 유형과 효과 식별 |
| 카드 인스턴스 | 고유 ID, 공개 여부와 현재 사용 상태 |
| 전투 모델 | 사용 가능 검사, 선택 수명주기, 효과 순서, 차례와 승패 |
| 손패·덱 | 카드 찾기·이동·임시 분리·덱 위 반환의 불변 조건 |
| 세션 | 전투 모델 API 전달 |
| 진행 시스템 | 런 전투 입력 전달과 종료·영혼 동기화 |
| Presenter/View | 읽기 전용 표시와 사용자 입력 전달 |
| 적 정책 | 보위 나이프 사용 판단과 적 카드 사용 판단. 비버스트 카드 폐기는 규칙 계층이 강제 |

효과 처리 계층은 카드와 계약이 함께 사용할 수 있는 순수 C# 경계로 둔다. 계약은 일반 카드와 구분되는 규칙 상태지만, 선택한 악마 카드는 일반 카드와 같은 크기의 앞면 월드 카드로 플레이어 앞에 놓고 호버 시 능력과 대가를 표시한다. 최종 월드 계약 UI는 별도 후속 구현 범위다.

## 10. 완료 조건

다음을 모두 만족하면 카드 사용 시스템의 현행 기획 이관을 완료한다. 위협용 해머는 2026-07-22에 상대 공개 카드를 버리는 현행 규칙으로 코드·적 AI·GameScene 표시·자동 테스트 이관을 마쳤다. 최신 악마 연계 효과와 자동 카드 숫자는 각각 DC-R·AC-RV 후속 구현 범위다.

- 일반 수동 카드 4종이 문서의 조건과 처리 순서대로 작동한다.
- 잘못된 상태·카드·선택은 전투 상태를 바꾸지 않는다.
- 비공개 카드 사용은 공개 이후 취소할 수 없다.
- 카드 한 장은 다시 뽑기 전까지 한 번만 사용된다.
- 선택 중 다른 전투 행동이 실행되지 않는다.
- 효과 중 버스트가 즉시 라운드와 전투 결과에 반영된다.
- 독립 전투와 런 전투에서 같은 카드 사용 결과가 나온다.
- 런 전투 종료 시 지속 영혼과 진행 상태가 정확히 동기화된다.
- 카드 효과가 상대 비공개 정보를 표시 모델과 로그에 노출하지 않는다.
- 신규·기존 EditMode 테스트, 두 테스트 씬과 실제 화면 흐름이 통과한다.
- AI 활용 기술서와 이천서의 실제 담당 기록을 단계마다 갱신한다.

## 11. 임시 결정과 재검토 시점

| 결정 | 현재안 | 재검토 시점 |
| --- | --- | --- |
| 해머 버림 대상 | 상대의 공개 카드 1장 | 확정, 코드·UI 이관 완료 |
| 적 해머 대상 선택 | 공개 카드 중 숫자가 가장 큰 카드 | 프로토타입 AI 정책, 밸런스 검증 시 |
| 나이프 비버스트 처리 | 방금 강제 드로우한 카드를 항상 즉시 버림 | 확정, 코드·테스트 이관 완료 |
| 사용 후 카드 위치 | 별도 문구가 없으면 손에 유지 | 카드 밸런스 테스트 후 |
| 사용 승인 후 취소 | 불가 | 최종 카드 UI UX 검토 시 |
| 콘텐츠 정의 방식 | 순수 C# 정의·카탈로그 | 카드 보상·편집 도구 구현 전 |
| 효과 공통화 수준 | 타입이 있는 명령과 명시적 처리기 | 계약 효과 요구사항 확정 후 |

## 12. 현행 카드 개별 재검토

폴드 삭제 이력을 다시 대조한 결과, 카드 가운데 폴드에 직접 의존했던 것은 독극물과 루시퍼다. 독극물은 영혼 지불 대신 즉시 스탠드할 수 있게 바꾸고 기존 영혼 지불·승리 보상 구조를 유지한다. 루시퍼 개정 규칙은 구현·검증 이력으로 보존하지만 루시퍼 자체는 현행 프로토타입에서 출현하지 않는다.

### 12.1 일반 카드

| 카드 | 검토 상태 | 후속 결정 |
| --- | --- | --- |
| 에이스 | 문구 보완 | 라운드 종료뿐 아니라 모든 합계·버스트 판정에서 1 또는 11을 자동 선택하는지 확정 |
| 효과 없는 2·3·4 | 유지 | 기본 확률과 효과 카드 밀도를 조절하는 기준 카드로 사용 |
| 수정 구슬 | 유지 | 자동 발동 카드가 후보에 포함될 때 발동 순서만 추가 결정 |
| 위협용 해머 | 대상 변경·이관 완료 | 상대 공개 카드 1장을 버리며 자신의 카드는 비용으로 요구하지 않음. 코드·적 AI·GameScene 표시·자동 테스트 반영 완료 |
| 리볼버 | 유지·밸런스 검증 | 추론 확률과 적 비공개 카드 분포에 따른 성공률 측정 |
| 보위 나이프 | 현행 판정 이관 완료 | 강제 히트 뒤 공개 합만 버스트 판정에 사용하고, 비버스트면 강제 드로우 카드를 즉시 버림 |

### 12.2 악마 카드

| 카드 | 검토 상태 | 후속 결정 |
| --- | --- | --- |
| 사탄 | 개정 확정·코드 이관 완료 | 활성 계약 카드 윗면의 숫자 2개 추측과 아랫면의 강제 히트가 일반 카드 효과 경계를 재사용하되 별도 숫자 카드는 생성하지 않음 |
| 벨페고르 | 현행 유지 | 덱 맨 위 미리보기 뒤 히트 또는 덱 맨 아래 이동·차례 종료 |
| 바알제붑 | 개정 확정·코드 이관 완료 | 버스트 시 영혼 1을 지불한 뒤 계약 소유자가 자신의 공개 1장·상대 공개 1장을 직접 선택해 버리고 생존 |
| 아스모데우스 | EP-R04 이관 완료 | 차례 시작 선택형 상대 강제 히트는 유지하고 숫자 7 이하 수동 카드 사용을 금지한다. 숫자 7은 제한되고 8은 허용되는 경계를 회귀 테스트로 고정한다. |
| 루시퍼 | 현행 확정·코드 이관 대기 | 무작위 악마 5장 중 하나를 추가 계약하고 영혼 1과 선택 악마의 대가 적용 |
| 마몬 | 개정 확정·코드 이관 완료 | 재굴림은 일반 행동을 대신하고 즉시 차례 종료, 라운드 종료 직전 합계 가산 여부 선택 |
| 레비아탄 | 개정 확정·코드 이관 완료 | 첫 리볼버 예측 실패 뒤에만 한 번 더 예측하며, 두 예측이 모두 실패하면 영혼 1 손실 |
| 메피스토펠레스 | 개정 확정·코드 이관 완료 | 보위 나이프 결과 상대 공개 합이 21을 넘으면 버스트시키고 실패 시 자신의 비공개 카드 공개 |
| 파이몬 | 개정 확정·코드 이관 완료 | 상대 버스트 시 양쪽 덱 중 하나의 위 2장을 보고 건너뛰거나 한 장을 현재 전투 동안 추방하고, 임시 승자 한 명의 18 이하 대가만 한 번 판정 |
| 벨리알 | 현행 확정·코드 이관 완료 | 차례 시작에 가져온 상대 공개 카드를 즉시 사용하며 현재 전투 동안 계약 소유자 카드로 처리하고 전투 종료 시 원소유권 복구 |
| 바포메트 | 개정 확정·코드 이관 대기 | 양측 오망성 소진 시 해당 대상을 버스트시키고 덱 초기화 뒤 정해진 수량 재삽입 |
| 아자젤 | 현행 개정·코드 이관 완료 | 계약 즉시와 정상 히트 뒤 공개 수동 효과 카드를 왼쪽부터 각각 한 번 사용하고 동일 숫자 공개 유입 시 자동 발동보다 먼저 자기 버스트 |

### 12.3 자동 발동 카드

| 카드 | 검토 상태 | 후속 결정 |
| --- | --- | --- |
| 독극물 1 | 효과 유지·숫자 이관 대기 | 즉시 스탠드하거나 영혼 3개·부족 시 보유 영혼 전부를 지불. 지불 후 라운드 승리 시 영혼 5 회복 |
| 부활초 2 | 개정 이관 대기 | 양쪽이 독립적으로 영혼 1을 버릴 수 있고, 지불한 참가자만 자기 손패를 재배분 |
| 거짓말 탐지기 3 | 현행 유지 | 선언 범위와 `이상/미만` 응답을 UI에 명확히 표시 |
| 화염 방사기 4 | 효과 유지·숫자 이관 대기 | 스탠드하지 않은 양측의 공개 카드 선택을 소유자부터 순서대로 처리 |
| 회중시계 5 | 효과 유지·숫자 이관 대기 | 사용 완료 수동 카드만 재활성화하고 자동 카드·자기 자신은 대상으로 삼지 않음 |

### 12.4 특수 카드

| 카드 | 검토 상태 | 후속 결정 |
| --- | --- | --- |
| 사탄 윗면/아랫면 계약 상태 | 일반 카드 모델 폐기 | 숫자 8·10 특수 카드로 생성하지 않고 활성 계약 상태에서 현재 윗면/아랫면과 사용 가능 여부를 관리한다. 일반 카드의 앞면/뒷면 공개 상태와는 무관하다. |
| 오망성 | 개정 확정·코드 이관 대기 | 상대 5장·자신 3장을 전투 덱에 삽입하고 각 소진 시 버스트·덱 초기화·재삽입 처리 |

### 12.5 폴드 의존 카드의 확정 대체안

#### 독극물 — 숫자 1, 자동 발동

발동 시 다음 중 하나를 선택한다.

- 지금 즉시 스탠드한다.
- 영혼 3개를 담보로 건다. 현재 영혼이 3개보다 적다면 현재 보유한 영혼을 모두 건다.

영혼을 담보로 걸고 해당 라운드에서 승리하면 영혼을 5개로 돌려받는다. 회복은 최대 영혼을 넘을 수 없다. 즉시 스탠드와 스탠드 불가 효과가 동시에 적용될 때의 우선순위는 후속 공통 판정 설계에서 확정한다.

#### 루시퍼 — 악마 카드

계약 즉시 무작위 악마 카드 5개 중 하나를 선택해 계약한다. 루시퍼의 대가로 영혼 1개를 잃으며, 선택한 악마의 능력과 대가도 모두 적용한다. 후보 출처, 루시퍼 자신과 중복 악마의 등장 여부는 악마 카드 덱 초기 구성과 함께 확정한다.

### 12.6 CU-M05 적 카드 호버 정보

- 플레이어가 이미 볼 수 있는 적 공개 카드는 호버 시 숫자·이름과 카드 효과 설명을 표시한다.
- 효과가 없는 공개 카드도 숫자·이름은 표시한다.
- 효과 사용 등으로 앞면 공개된 비공개 역할 카드도 확정 공개 정보이므로 같은 호버 정보를 표시한다.
- 아직 앞면 공개되지 않은 적 비공개 카드는 숫자·이름·효과 설명을 모두 표시하지 않는다.
- 호버 정보는 표시 계층의 읽기 전용 기능이며 적 카드를 플레이어가 사용 가능하게 만들지 않는다.
- 기존 공용 HUD 배지와 포인터 레이캐스트를 재사용하고 씬·프리팹 직렬화는 변경하지 않는다.

### 12.7 CU-M06 리볼버 사용 연출 흐름

- 플레이어가 리볼버 사용을 승인하면 숫자 선택 전에 리볼버 오브젝트를 활성화하고 준비 애니메이션을 시작한다.
- 준비 연출 중 1~10 예측 선택 UI를 표시하며, 선택이 완료될 때까지 리볼버를 숨기지 않는다.
- 예측 성공은 플레이어 성공 사격, 실패는 플레이어 실패 사격 애니메이션으로 이어진다.
- 연출은 전투 판정 결과를 소비할 뿐 성공·실패를 다시 계산하지 않으며, 같은 카드 사용 결과를 중복 재생하지 않는다.
- 기존 `Revolver_Anim` 프리팹·Animator Controller·직렬화 참조를 재사용하고 씬·프리팹 파일은 수정하지 않는다.

### 12.8 CU-M07 레비아탄 조건부 재예측

- 첫 예측 성공 시 성공 연출로 끝내며 상대 버스트가 다른 계약으로 방지되어도 다시 예측하지 않는다.
- 첫 예측 실패 시 실패 연출을 먼저 온전히 보여 준 뒤 리볼버 준비 상태와 1~10 선택을 새로 연다.
- 두 번째 예측은 일반 리볼버와 같은 성공·실패 판정을 사용한다. 성공하면 대가가 없고, 두 번 모두 실패하면 레비아탄 소유자가 영혼 1을 잃는다.
- 두 번째 실패 대가로 영혼이 0이 되면 즉시 사망한다. 두 선언은 독립적이며 같은 숫자를 다시 선택할 수 있다.
- 연출은 `Ready → ResolvedWithRetry → Ready → Resolved` 단계로 구분해 첫 실패 결과를 다음 선택 화면이 덮지 않도록 한다.

### 12.9 CU-M09 GameScene 해머 대상 직접 선택

- 플레이어가 해머 사용을 승인하면 별도 대상 버튼 대신 테이블의 유효한 적 공개 카드를 직접 클릭해 제거 대상을 확정한다.
- 대상 카드는 기존 상대 카드의 호버 확대·외곽선·숫자·이름·효과 배지를 그대로 사용한다.
- 뒷면 비공개 카드와 앞면이 공개된 비공개 역할 카드는 대상에서 제외한다. 비대상 카드 클릭은 상태를 바꾸지 않는다.
- 해머 선택 대기 HUD는 안내 문구만 표시하고 선택 버튼을 만들지 않는다. 리볼버·수정 구슬 등 다른 수동 선택 효과는 기존 버튼을 유지한다.
- `GameScene`의 기존 포인터 레이캐스트와 `CardView`를 재사용하며 씬·프리팹·머티리얼 직렬화는 변경하지 않는다.

### 12.10 CU-M10 사용 완료 카드 연필 X 표시

- `CardUseState.Used`인 플레이어 카드와 앞면 공개된 적 카드에 검은 흑연 X를 표시한다.
- 미공개 적 카드는 실제 사용 상태와 무관하게 X를 표시하지 않아 비공개 정보를 보존한다.
- 같은 카드가 미사용 상태에서 사용 완료로 바뀌면 두 획을 각각 0.175초 동안 순차적으로 그린다.
- 최초 표시와 풀에서 다른 카드 ID로 재사용된 경우에는 완성된 X를 즉시 표시한다.
- 회중시계로 `Used → Available` 재활성화되면 X를 즉시 제거한다. 아자젤 연속 사용은 카드를 곧바로 `Resolving`으로 전환하고 완료 뒤 다시 X를 표시한다.
- X는 읽기 전용 표시이며 카드 사용 가능 여부·호버·클릭·정렬 규칙을 변경하지 않는다.

## 13. 변경 기록

| 날짜 | 작성자 | 변경 |
| --- | --- | --- |
| 2026-07-31 | HONG | CU-M10에서 양측 공개 Used 카드에 검은 흑연 X를 표시하고 두 획 0.35초 그리기·재활성화 제거·미공개 적 상태 차단을 확정 |
| 2026-07-30 | HONG | CU-M09에서 GameScene 해머 대상 버튼을 제거하고 기존 적 카드 호버와 직접 클릭으로 유효한 상대 공개 카드만 선택하도록 확정 |
| 2026-07-30 | 이천서 | CU-M08에서 보위 나이프의 규칙 결과는 유지하되 강제 공개 드로우를 먼저 보여 주고, 비버스트이면 해당 물리 카드가 손패에서 빠져나가며 축소되는 폐기 연출을 뒤이어 표시하도록 결정 |
| 2026-07-29 | 이천서 | Notion v0.7에 따라 아스모데우스 제한을 7 이하로 재명세하고 사탄 상태를 앞뒷면이 아닌 윗면/아랫면 전환으로 용어 정리; 코드·테스트 미변경 |
| 2026-07-29 | 이천서 | CU-M07에서 레비아탄을 첫 실패 때만 재예측하는 규칙으로 개정하고 첫 실패 연출·재등장·두 번째 결과 및 두 번 실패 시 영혼 1 대가를 확정 |
| 2026-07-29 | 이천서 | CU-M06에서 리볼버 사용 승인 직후 준비 오브젝트를 표시하고 숫자 예측 뒤 성공·실패 사격으로 이어지는 GameScene 연출 흐름을 확정 |
| 2026-07-29 | 이천서 | CU-M05에서 공개된 적 카드와 앞면 공개된 비공개 역할 카드의 숫자·이름·효과 호버 정보를 허용하고, 아직 뒷면인 적 비공개 카드 정보는 계속 차단하도록 확정 |
| 2026-07-29 | 이천서 | CU-M04에서 비공개 역할과 앞·뒷면 표시 상태를 분리하고, 공개된 비공개 카드의 공개 합·공개 대상 제외, 최종 승부까지의 역할·왼쪽 위치 유지와 적 리볼버 확정 우선 대응을 기획·코드·테스트 기준으로 반영 |
| 2026-07-28 | 이천서 | 사탄·마몬·레비아탄·메피스토펠레스의 기존 개정 이관 상태를 바로잡고, DC-R04 3/5 파이몬 선택 추방·단일 임시 승자 대가와 벨리알 탈취·즉시 사용·전투 종료 소유권 복구의 코드 이관 완료를 기록 |
| 2026-07-27 | 이천서 | 최신 노션 기준으로 악마별 카드 연계, 메피스토펠레스의 보위 나이프 강화·대가, 사탄의 별도 권능 카드 폐기와 자동 카드 숫자 1~5를 반영하고 DC-R·AC-RV 코드 이관 대기로 구분 |
| 2026-07-19 | 이천서 | 카드 사용 시스템의 1차 범위, 카드 4종 규칙, 화면·효과 종료·기존 시스템 경계를 CU-00 구현 기준으로 확정 |
| 2026-07-19 | 이천서 | 리볼버은 정상 라운드의 단일 비공개 카드만 대상으로 하며 다중 비공개 카드 규칙을 추가하지 않음을 명시 |
| 2026-07-20 | 이천서 | 일반 수동 카드 4종의 구현·화면 연결·독립 및 런 전투 반복 검증을 마쳐 1차 범위를 CU-06 완료 상태로 갱신 |
| 2026-07-21 | 이천서 | 위협용 해머 대상을 상대 공개 카드 1장으로 바로잡고 기존 구현과의 차이를 코드 이관 대기로 기록 |
| 2026-07-21 | 이천서 | 폴드 의존 카드인 독극물·루시퍼를 재설계 전까지 제외하고 일반·악마·자동 발동·특수 카드를 개별 재검토 |
| 2026-07-22 | 이천서 | 독극물을 즉시 스탠드 또는 기존 영혼 베팅으로 개정하고, 루시퍼 대가를 영혼 1과 선택 악마의 대가 동시 적용으로 확정 |
| 2026-07-22 | 이천서 | 위협용 해머를 상대 공개 카드 제거·스탠드 취소와 비공개 교체 규칙으로 코드·적 AI·GameScene 표시까지 이관하고 전체 EditMode 308/308 통과를 확인 |
| 2026-07-22 | 이천서 | 규칙 손패 순서·카드 ID·정보 은닉을 유지한 채 양측 비공개 카드를 플레이어 시점 최좌측에 표시한 CU-M03 상태를 반영 |
| 2026-07-26 | 이천서 | 계약 후보·활성 악마를 일반 카드 크기의 앞면 카드로 표시하고 호버 시 능력과 대가를 함께 보여 주는 UI 경계 반영 |
